{
  "summary": "Research Nav2 autonomy under orchard canopy, Korean tall-spindle apple orchard geometry, and simulated apple disease rendering",
  "agentCount": 11,
  "logs": [
    "Addendum research: nav2, apple, disease",
    "3/3 dimensions returned"
  ],
  "result": {
    "brief": "# ADDENDUM DECISION BRIEF\n## AgileX SCOUT MINI + Livox MID-70, Korean high-density apple orchard (세장방추형), ROS 2 Jazzy + Gazebo Harmonic\n\n**Standing rule applied throughout: where a verification verdict refutes or corrects a research finding, the VERDICT WINS.** Every place that happened is called out explicitly with a `VERDICT OVERRIDE` tag.\n\n---\n\n## 0. The six verdict overrides that changed the design\n\n| # | Research said | Verdict says | Design consequence |\n|---|---|---|---|\n| V1 | The 70.4° FOV is what breaks AMCL | AMCL has **no** 360° requirement (`nav2_amcl/src/amcl_node.cpp` computes `angle_min + i*angle_increment`, no full-circle check). The **hard** blockers are: AMCL subscribes to `sensor_msgs/msg/LaserScan` **only**; the non-repetitive rosette does not flatten into a stable planar scan; AMCL also needs a pre-built static occupancy grid, awkward in 노지 | AMCL is **off the critical path entirely**. Not \"degraded\" — structurally wrong tool. See §2. |\n| V2 | Nav2 costmaps don't persist outside FOV; add STVL for persistence | `nav2_costmap_2d/plugins/voxel_layer.cpp`: marking writes LETHAL, clearing happens **only** via `raytraceFreespace()` inside the FOV. Persistence is **already the default**. The failure mode is **stale, un-clearable phantom obstacles**, not missing ones | STVL's role **inverts**: `decay_time` is there to **expire** stale marks, not to retain them. Decay set to **8–15 s**, not the 15–60 s originally proposed. |\n| V3 | Use SmacPlannerHybrid (min turning radius 0.4–0.6 m) at headlands | Nav2's own selection table: Hybrid-A* → \"Non-circular or Circular **Ackermann**, ... **Legged**\". SmacPlanner2D → **circular** differential only (no SE2 footprint check). Only **SmacPlannerLattice** covers \"Non-circular Differential\" — and its shipped control sets \"were generated using the 5cm **Ackermann** files\" | **Do not use Hybrid-A***. Headland default = NavFn + RotationShim + MPPI. Upgrade path = SmacPlannerLattice with a **custom-generated differential control set** including rotate-in-place primitives (`rotation_penalty`). |\n| V4 | MPPI is the modern choice vs \"legacy DWB\" | DWB is **not** deprecated (`dwb_core` released at Jazzy 1.3.12). And **MPPI is already the Jazzy default** — `nav2_bringup/params/nav2_params.yaml` has shipped `nav2_mppi_controller::MPPIController` since tag 1.3.0. Default planner is **NavFn**, not Smac. MPPI's own README warns of **jitter in narrow corridors** if `repulsion_weight` is high relative to `inflation_radius` | MPPI is not a \"choice\", it's the default. The real work is the narrow-corridor tuning, and §2 gives the numbers. |\n| V5 | \"Rendered lesion texture lacks fine-scale statistics, so sim disease training won't transfer\" | **PARTIALLY_TRUE / mechanism refuted.** Klein et al., *Front. Plant Sci.* 15 (2024), doi:10.3389/fpls.2024.1360113 trained **exclusively on Blender-rendered images** and hit **89.6%** on real greenhouse tomato photos; the authors state \"photorealism ... is **not the main quality driver**\". Training on real PlantVillage photos was \"barely better than random guessing\" on their real test set. SDFormat 1.11 `<pbr><metal><albedo_map>` takes a **user-supplied photo file** — lesion texture statistics are authorable, not shader-synthesized | The justification for not sim-training the deployed disease model **changes**. It is no longer \"renders look fake\". It is (a) GSD physics, (b) biological species-ID ceiling, (c) domain shift that real data doesn't escape either. And synthetic-primary training for coarse **symptom** detection becomes defensible if you invest in **content diversity + iterative refinement**, not photorealism. See §5. |\n| V6 | Alley free width ~2.3–2.6 m; keep inflation below 0.7 m; 1.475 m clearance per side | Canopy width is **0.9–1.5 m** (Robinson: tree diameter 0.9–1.2 m; 한국사과협회: 나무 폭 1.5 m 내외). At 3.0–3.5 m rows the free alley is **1.6–2.4 m** | Worst case free alley = **2.0 m**, giving **0.725 m** per side, not 1.475 m. `inflation_radius` drops to **0.40 m** (hard cap 0.55 m). This is a materially tighter corridor than the original brief assumed. |\n\nTwo further verdict facts that are project-blocking and were not in any finding's headline:\n\n- **The MID-70 has no ROS 2 Jazzy driver path.** `livox_ros_driver2` README \"Supported LiDAR list\" = **HAP and Mid360 only**. `livox_ros_driver` (v1) = ROS 1, Ubuntu 14/16/18. `livox_ros2_driver` = v0.0.1-beta, Dashing/Foxy/Humble. MID-70 appears in none of them for Jazzy.\n- **The MID-70 has no built-in IMU** (livoxtech.com/mid-70/specs has no IMU row). Every LIO package (FAST-LIO2, Point-LIO) mandates IMU input. Point-LIO furthermore has **no ROS 2 branch at all** (`hku-mars/Point-LIO` branches: `master`, `point-lio-with-grid-map`; README \"tested on Ubuntu20.04 with noetic\").\n\n---\n\n## 1. Sensor suite verdict\n\n**Statement of record: the Livox MID-70 alone cannot drive full Nav2.** Not primarily because of the 70.4° aperture (V1), but because it emits point clouds only, its non-repetitive rosette does not flatten to a stable `sensor_msgs/LaserScan`, it has no IMU, it has no ROS 2 Jazzy driver, and a forward cone can never raytrace-clear the lateral/rear costmap cells it marks (V2). **The MID-70 is reclassified as a perception sensor.** Navigation runs on a separate sensor set.\n\nPoses are `x y z roll pitch yaw` in metres/radians relative to `base_link`, **with `base_link` at the ground-projected chassis centre, z = 0 at the ground plane**. If your Scout Mini URDF puts `base_link` at wheel-axle height, subtract 0.0875 m from every z.\n\n| # | Sensor (SDF `<sensor>` name / type) | Pose | Rate / config | Role | Why it exists |\n|---|---|---|---|---|---|\n| 1 | `nav_lidar_360` — `gpu_lidar` | `0.15 0 0.55 0 0 0` | 360 h-samples × 16 v-samples, h-FOV ±π, v-FOV ±0.26 rad (±15°), 0.15–30 m, **10 Hz** | Nav2 costmap marking+clearing; scan-matching; end-of-row detection backup | **Fills the entire Nav2 gap.** Restores lateral/rear raytrace clearing so stale marks can expire (V2). ~5,760 rays/frame — deliberately cheap. HW analogue: **Livox MID-360** (~USD 650–900), the one Livox unit `livox_ros_driver2` actually supports, and that driver **does** list Jazzy. Budget fallback: LD19 / RPLIDAR S2 2D. |\n| 2 | `mid70_perception` — `gpu_lidar` | `0.10 0.24 0.70 0 -0.175 1.571` (left-facing, pitched **up** 10°) | **100 × 100** samples, h/v FOV **±0.61436 rad**, 0.05–90 m, **10 Hz** = 10,000 pts/frame | Canopy structure, trunk detection, fruit 3D localization, **camera standoff control** | Faithful 100 kpts/s budget. **Side-facing, not forward**, because (a) row centreline comes free from sensor 1, (b) the two-sided imaging requirement (§5) already dictates a canopy-facing rig, (c) it resolves the mount conflict between nav-optimal (low/level/forward) and perception-optimal (high/tilted/lateral) *before* the URDF is written. |\n| 3 | `imu_link` — `imu` | `0 0 0.10 0 0 0` | 200 Hz, gyro σ 2e-4, accel σ 1.7e-2 | `robot_localization` local + global EKF | **Mandatory.** MID-70 has no IMU; both EKFs and any future LIO need one. On HW this is a separate part with its own extrinsic calibration and time-sync burden. |\n| 4 | wheel odometry — `gz::sim::systems::DiffDrive` | n/a | 50 Hz, `<publish_tf>false</publish_tf>` | Twist source for local EKF; the **only** along-row (X) constraint in-row | Publish odom from **exactly one** source. Do not also run `gz_ros2_control` + `diff_drive_controller`. |\n| 5 | `navsat` — `navsat` (+ `gz::sim::systems::NavSat`, world `<spherical_coordinates>`) | `-0.20 0 0.60 0 0 0` | 5 Hz | Global EKF input **in headlands only** | Gated hard in-row (§2). World must declare the NavSat system plugin. |\n| 6 | `cam_canopy_left` — `camera` | `0.05 0.26 1.20 0 -0.209 1.571` (up 12°) | **1920×1080**, h-FOV 1.047 rad (60°), 10 Hz, near 0.1 / far 30 | Primary imaging: fruit counting + disease symptoms | At **1.0 m standoff → 0.60 mm/px**. Two-sided coverage is non-negotiable (single-side visibility 40.85–79.83%, Roy/Isler arXiv:1808.04336). |\n| 7 | `cam_canopy_right` — `camera` | `0.05 -0.26 1.20 0 -0.209 -1.571` | identical | Mirror of #6 | With L+R cameras, **one boustrophedon pass over all M+1 alleys images both sides of all M rows**. Halves mission time vs a single-sided rig. |\n| 8 | `cam_forward_rgbd` — `rgbd_camera` | `0.30 0 0.45 0 0 0` | 640×480, h-FOV 1.204 rad (69°), 10 Hz, 0.2–10 m | End-of-row detection, headland trigger, tall-grass/vegetation traversability classification | Reproduces the GNSS-free headland approach (*Machines* 11(1):84 — 100% row-end detection success, 0.54 m mean error, within 7 m). Also the first-line answer to arXiv:2407.18535 (grass marked as lethal). |\n| 9–12 | `semseg` / `panoptic` (`segmentation`), `boxes_visible_2d` / `boxes_full_2d` (`boundingbox_camera`) | **Co-located with #6**, identical pose and intrinsics | 1920×1080, **1–2 Hz or triggered** | Training-label ground truth | See §6. Must share extrinsics **and** `frame_id` with #6 or masks will not align. |\n\n**Sensors deliberately NOT included:** no second MID-70; no thermal (no use case identified); no multispectral (arXiv:2302.08818 reports operational failure from open-field exposure balancing — and Gazebo has no auto-exposure to reproduce it anyway).\n\n---\n\n## 2. Localization & navigation architecture\n\n### 2.1 The core localization decision\n\nThe row is **deliberately self-similar along X**. No sensor recovers along-row position from a mid-row observation — this degrades AMCL, slam_toolbox loop closure, and FAST-LIO2 pose-graph optimization *identically*. FAST-LIO's own authors say it: \"the LiDAR-based solution easily degenerates ... This problem is more obvious when the LiDAR has a small FoV\" (arXiv:2010.08196 §I).\n\n**So stop trying to observe X continuously.**\n\n| Axis | In-row source | Headland source |\n|---|---|---|\n| **Y (lateral), yaw** | Continuous, from row-centreline fit to trunk points (`nav_lidar_360` + `mid70_perception`) | GNSS + EKF |\n| **X (along-row)** | **Dead-reckoned** from wheel odom + IMU; **discretely reset** at row-entry and row-exit events detected by `cam_forward_rgbd` | GNSS + EKF |\n| **map→odom** | `robot_localization` global EKF, GNSS covariance inflated ×100 or gated off on FLOAT/DGPS status | `robot_localization` global EKF, GNSS trusted |\n\nX drift over a 45 m row at 1–2% wheel-odom error = 0.45–0.9 m, corrected at the far end by row-exit detection. That is acceptable because **X error does not cause collisions in a row** — Y error does, and Y is well observed.\n\n### 2.2 Pipeline, sensors → `/cmd_vel`\n\n```\ngz-sim Harmonic (ogre2)\n  └─ ros_gz_bridge (parameter_bridge, YAML config)   ros-jazzy-ros-gz \n       ├─ /clock                                      → use_sim_time:=true on EVERY node\n       ├─ /scan_360 (PointCloud2)\n       ├─ /mid70/points (PointCloud2)\n       ├─ /imu, /odom, /navsat/fix, /cam_*/image_raw\n       │\n  pointcloud_to_laserscan_node   (pkg: pointcloud_to_laserscan)\n       inf_is_valid: true   min_height: 0.20   max_height: 1.00\n       → /scan                                        [360-only; NEVER the MID-70]\n       │\n  robot_localization  ekf_node \"ekf_odom\"   → odom→base_link   (wheel twist + IMU)\n  robot_localization  navsat_transform_node → /odometry/gps\n  robot_localization  ekf_node \"ekf_map\"    → map→odom         (+ /odometry/gps, gated)\n  row_centerline_node  (custom, ~200 LOC)   → geometry_msgs/PoseWithCovarianceStamped\n                                               (Y+yaw only, huge X covariance) → ekf_map\n  row_event_node       (custom, ~150 LOC)   → discrete X reset at row entry/exit\n       │\n  nav2_route (Route Server, Jazzy)  — topological graph, typed edges\n       │\n  nav2_planner        ├─ \"InRow\"    : RowCenterlinePlanner   (custom nav2_core plugin, ~300 LOC)\n                      └─ \"Headland\" : nav2_navfn_planner::NavfnPlanner\n  nav2_controller     RotationShimController → nav2_mppi_controller::MPPIController\n  nav2_costmap_2d     StaticLayer? no · STVL · InflationLayer\n  nav2_bt_navigator   BT selects planner_id + goal_checker per route edge type\n       │\n       └─→ /cmd_vel  →  gz DiffDrive\n```\n\n**Stage-0 scaffold (do this first, it is not optional):** a ~50-line node that reads gz model pose and publishes a perfect `map→odom`. Tune MPPI, inflation, route graph and row geometry against a perfect localizer, so that when navigation misbehaves you know which layer is broken. Swap in the real stack at step 6.\n\n### 2.3 Key config values\n\n```yaml\n# --- footprint (polygon, NOT radius) --------------------------------\nfootprint: \"[[0.34,0.30],[0.34,-0.30],[-0.34,-0.30],[-0.34,0.30]]\"\n#   Scout Mini 0.627 x 0.550 -> circumscribed 0.417, inscribed 0.275\n#   circular 0.417 would throw away 0.142 m/side out of only 0.725 m  [V6]\n\n# --- local costmap --------------------------------------------------\nresolution: 0.05 ; rolling_window: true ; width/height: 6.0\nplugins: [\"stvl_layer\", \"inflation_layer\"]\nstvl_layer:                            # spatio_temporal_voxel_layer 2.5.5 (Jazzy)\n  plugin: \"spatio_temporal_voxel_layer/SpatioTemporalVoxelLayer\"\n  voxel_decay: 12.0                    # SECONDS. 8-15. Purpose = EXPIRE stale\n                                       # un-clearable marks, NOT retain them.  [V2]\n  decay_model: 0                       # linear\n  voxel_size: 0.05 ; mark_threshold: 0\n  observation_sources: scan360 mid70_ground_guard\n  scan360: {min_obstacle_height: 0.20, max_obstacle_height: 1.00,\n            clearing: true, marking: true, obstacle_range: 10.0}\n#   min 0.20 rejects mown alley grass (arXiv:2407.18535)\n#   max 1.00 rejects overhanging canopy the mast can brush through\ninflation_layer:\n  inflation_radius: 0.40               # HARD CAP 0.55. Free alley worst case\n                                       # 2.0 m -> 0.725 m/side.  [V6]\n  cost_scaling_factor: 3.0\n\n# --- global costmap -------------------------------------------------\nresolution: 0.10 ; rolling_window: true ; width/height: 40.0\n#   0.05 m over a multi-hectare orchard is a memory problem\n\n# --- controller_server ----------------------------------------------\nFollowPath:\n  plugin: \"nav2_rotation_shim_controller::RotationShimController\"\n  primary_controller: \"nav2_mppi_controller::MPPIController\"\n  angular_dist_threshold: 0.785 ; forward_sampling_distance: 0.5\n  rotate_to_heading_angular_vel: 0.6\n  # MPPI:\n  motion_model: \"DiffDrive\"            # correct for 4WD skid-steer\n  batch_size: 2000 ; time_steps: 56 ; model_dt: 0.05\n  vx_max: 0.6                          # imaging speed limit, see below\n  vx_min: -0.20 ; wz_max: 1.0\n  ObstaclesCritic:\n    repulsion_weight: 1.0              # LOWERED from default 1.5: MPPI README\n                                       # warns of \"jitter in narrow corridors\" [V4]\n    critical_weight: 20.0\n    inflation_radius: 0.40             # MUST mirror the costmap layer exactly\n    cost_scaling_factor: 3.0\n  PathAlignCritic: {weight: 16.0}      # raised: stay on the centreline\n\n# --- planner_server (two plugins configured simultaneously) ----------\nplanner_plugins: [\"InRow\", \"Headland\"]\nInRow:    {plugin: \"orchard_nav::RowCenterlinePlanner\"}\nHeadland: {plugin: \"nav2_navfn_planner::NavfnPlanner\", tolerance: 0.25}\n#   NOT SmacPlannerHybrid: Nav2's own table lists it Ackermann/Legged only. [V3]\n#   Upgrade path if wheel scrub proves to matter: SmacPlannerLattice with a\n#   custom-GENERATED differential control set incl. rotate-in-place primitives.\n\n# --- behavior_server -------------------------------------------------\n#   Keep Spin (sweeps sensors, cheapest way to repopulate the costmap).\n#   Keep BackUp with backup_dist <= 0.15 — the \"delete BackUp\" advice was\n#   conditioned on zero rear coverage, which sensor #1 removes.\n```\n\n**In-row speed cap 0.6 m/s** is an *imaging* constraint, not a navigation one: at 0.6 mm/px and a 1/1000 s global shutter, 0.6 m/s gives 0.6 mm of smear = 1 px. Headland 1.0 m/s.\n\n**Verify before designing around it:** `apt list ros-jazzy-nav2-route` on the target machine. Jazzy Nav2 is at **1.3.12**. If `nav2_route` is absent from binaries, fall back to a plain `NavigateThroughPoses` BT over waypoints — the route graph is a convenience, not a dependency.\n\n**FAST-LIO2 / Point-LIO: research spur only, never plan of record.** Point-LIO has no ROS 2 branch. `FAST_LIO` ROS2 branch says \"ROS >= Foxy (Recommend ROS-Humble)\" and depends on `livox_ros_driver2`, which does not support the MID-70 at all. Both require Livox `CustomMsg` per-point timestamps, which a stock `gpu_lidar` `PointCloud2` does not provide — you would have to synthesise `CustomMsg` in sim yourself. And the MID-70 has no IMU. Four independent blockers.\n\n---\n\n## 3. Orchard geometry spec\n\nDrive the generator from this table. Everything is a parameter; the \"Nominal\" column is the default profile (`sejang_bangchuhyeong`).\n\n| Parameter | Nominal | Range / variant | Source |\n|---|---|---|---|\n| Row spacing 열간 | **3.50 m** | 2.80–3.90 (키큰방추형 3.30) | RDA 농사로 cntntsNo=30663; 충북 ARES 세장방추형 2.8–3.5 |\n| In-row spacing 주간 | **1.50 m** | 1.00–1.80 (키큰방추형 1.00) | RDA: 3.5×1.5 → 190주/10a |\n| Density | **1,900 trees/ha** (190주/10a) | 1,460–3,570/ha | arithmetic check: 1000/5.25 = 190 ✓ |\n| Tree height 수고 | **3.20 m** | 3.0–3.5 (키큰방추형 4.0–4.5) | 세장방추형 standard |\n| Canopy width | **1.20 m** | 0.90–1.50 | Robinson 0.9–1.2; 한국사과협회 1.5 |\n| **Free alley width** | **2.30 m** | **2.00 m worst case** (1.5 m canopy) | **V6 — this drove `inflation_radius`** |\n| Canopy bottom height | 0.70 m | 0.60–0.80 | lowest feathers ~0.30 m at nursery |\n| Trunk base diameter | 0.070 m | 0.060–0.080 | Cornell min caliper 16 mm at planting |\n| Trunk top diameter | 0.020 m | at 3.2 m | linear taper + axial noise σ 0.01 m |\n| Feathers (lateral branches) | **28** | 20–40 | pendant, **−10° to −30°** from horizontal |\n| Feather length | 0.45 m | 0.30–0.60 (키큰 0.50) | defining tall-spindle trait |\n| Feather azimuth | golden angle **137.5°** stratified | | |\n| Feather z-range | 0.70 → 3.00 m | | |\n| Inline trellis post | **3.66 m** (12 ft), 0.70 m buried → **2.96 m** exposed, ⌀0.075 m | every **10.0 m** | Cornell cost table: 110 poles/acre ÷ 10 rows × 400 ft |\n| End anchor post | 1.83 m (6 ft), angled 30° outward | 2 per row | Cornell: 20 anchor poles/acre |\n| Trellis wires | **3**, at **1.00 / 2.00 / 2.90 m**, ⌀2.5 mm | **LOW CONFIDENCE — inferred, not sourced** | visual only, no collision; 2.5 mm is below sim ray density anyway |\n| Rows × trees — **dev plot** | **4 × 20** | build this first, measure RTF | |\n| Rows × trees — full plot | 6 × 30 | | |\n| Orchard footprint (full) | **21.0 m × 55.5 m ≈ 0.117 ha** | 5 gaps ×3.5 + 2 outer alleys; 30×1.5 + 2×6.0 headland | |\n| **Headland width 선회 공간** | **6.0 m** | ≥5.0 m hard minimum | *Machines* 11(1):84 — maneuvering failed below 5 m with aligned rows |\n| Apples per tree (mature) | **60** | 50–100; year-2 15–20, year-3 50–60 | Cornell training table; 771 bu/ac ÷ 1320 trees ÷ 0.19 kg ≈ 60 |\n| Apple diameter | **0.075 m** | 0.060–0.085 | Korean Fuji avg 75 mm |\n| Cluster size distribution | 75.3% singles, rest 2–4 | | arXiv:1808.04336 Fig 21(b) |\n| Apple radial placement | 0.15–0.55 m from trunk axis, z 0.80–3.00 m | **inside** the leaf volume | see §4 occlusion target |\n| **Target single-side visible fraction** | **55–65%** | must land in **40.85–79.83%** | arXiv:1808.04336 — validate with `full_2d` vs `visible_2d` boxes |\n| Ground: alley | mown grass, textured plane, no per-blade collision | ambientCG Grass004 (CC0) | |\n| Ground: under-row strip | **1.20 m** bare soil (청경), centred on row | 0.90–1.50; 부초 mulch variant on slope | 부분초생재배: 열간 초생 / 수관하부 청경 |\n| Slope variant 경사지 | 0% flat default | 0–10% option → forces 부초 mulch | many Korean apple orchards are on slopes |\n| Scene budget | ~11k tris/tree → **~2.0 M tris** at 180 trees | apples at 80 tris (icosphere subdiv 1), **not** 320 | validate empirically, this is a planning estimate |\n\n---\n\n## 4. Tree asset pipeline\n\n**Decision: procedural generation. There is no download that shortcuts this.** Confirmed by direct Fuel REST API query (`fuel.gazebosim.org/1.0/models`) across 13 terms: Fuel contains **zero** apple, orchard, vineyard or crop models. Only `OpenRobotics/Oak tree` (CC0), `OpenRobotics/Pine Tree` (CC0), `shrijitsingh99/Juniper Tree` (CC0), `facugu123/Tree` (CC BY 4.0) — generic forest species, baked-in foliage, no fruit. Usable only as headland windbreak filler. Every Sketchfab/BlenderKit apple tree is an ornamental round-canopy shade tree — wrong architecture, no per-fruit hierarchy.\n\n### 4.1 The critical structural decision\n\nFruit must be individually addressable for panoptic instance IDs. **But do not make each apple a `<model>` or a nested `<include>`** — gz-sim issue **#1579** (open since 2022-07-07) breaks panoptic mode with nested labeled includes, returning zeros in `labels_map` and duplicate colours for distinct copies. And runtime-spawned models silently lose their labels.\n\n**Therefore:**\n- One tree = **one flat, top-level `<include>`** in the world SDF.\n- Inside that model: 1 tree-body `<visual>` (trunk + feathers + leaf cards, one merged `.dae`) + **N apple `<visual>`s**, each referencing the **same** 80-tri `apple.dae` mesh (shared via `gz::common::MeshManager` → VRAM stays flat) at its own `<pose>`, each carrying its **own** `<plugin filename=\"gz-sim-label-system\">`.\n- Instance IDs are assigned per-visual automatically. 60 apples × 180 trees = **10,800 visuals**. VRAM flat, draw calls linear — this is the performance risk, see §9.\n- **LOD:** only the rows you are actively imaging need per-apple visuals. Background rows get a single merged fruit submesh. Make this a generator flag.\n\n### 4.2 Blender 4.x headless `bpy` generator\n\n```\ngen_tree.py   --height 3.2 --canopy_w 1.2 --n_feathers 28 --n_apples 60 --seed N\n  1. trunk    : tapered cylinder, 10 radial segs, ⌀0.070→0.020, axial noise σ0.01\n  2. feathers : 28 cylinders, 6 radial segs, len 0.30–0.60,\n                pitch −10..−30°, azimuth k·137.5°, z 0.70→3.00\n  3. foliage  : alpha-tested leaf-CLUSTER cards (5–20 leaves per quad in a\n                2048² atlas), 150–400 cards/tree @ 2 tris.  NOT per-leaf cards\n                (~4,000 tris/tree vs ~600 for equivalent visual density)\n  4. apples   : icosphere subdiv 1 (80 tris), ⌀0.075, placed by rejection\n                sampling INSIDE the leaf volume to hit the 55–65% single-side\n                visible target — NOT conveniently on the canopy surface\n  5. export   : COLLADA .dae  (gazebosim.org/api/sim/9/model_and_optimize_meshes.html\n                explicitly recommends COLLADA for visual geometry; glTF/FBX\n                are not mentioned)\n  6. emit     : ground-truth JSON  {tree_id, apple_id, world_xyz, diameter,\n                cluster_id, label_id}\ngen_world.py  → FLAT world SDF, one <include> per tree, Label plugins baked in\n```\n\n**Skip Sapling Tree Gen / Modular Tree.** They produce naturalistic branching; a trained spindle is a deliberately artificial shape, easier to script directly than to coax out of Sapling's parameters.\n\n### 4.3 Named assets and licences\n\n| Asset | Licence | Use |\n|---|---|---|\n| **ambientCG** (ambientcg.com) — Bark012, Grass004, Ground037 | **CC0**, whole library, no attribution | bark, alley grass, bare strip soil |\n| **Poly Haven** (polyhaven.com) | **CC0**, whole library | HDRIs, additional ground |\n| `FieldRobotEvent/virtual_maize_field`, branch `ros2-gz` | **GPL-3.0** ⚠️ | **Read for architecture ONLY.** Solves the identical structural problem (procedural rows → SDF, Jazzy + gz, with CI). Copying source makes your generator GPL-3.0. |\n| `tduboudi/IAMPS2019-Procedural-Fruit-Tree-Rendering-Framework` | **MIT** ✅ | Safe starting script. Caveat: scatters fruit as random particles (authors flag it as unnatural) and outputs images, not meshes. |\n| OrchardBench, arXiv:2607.06337 | CC BY 4.0 (paper) | Mine the L-system + fruit-on-stem-tether methodology. Runs on Newton/MuJoCo-Warp, **not Gazebo**; asset pack release unconfirmed. |\n| Fuel `OpenRobotics/Oak tree`, `Pine Tree`, `Juniper Tree` | CC0 | headland windbreak filler only |\n\n### 4.4 Effort\n\n| Stage | Days |\n|---|---|\n| v0 crude: trunk + feather cylinders + apple spheres, no leaves, flat SDF emitter, GT JSON | **1–2** |\n| Leaf-cluster cards + 2048² atlas + bark/leaf/soil textures | 2–3 |\n| Trellis posts, wires, anchors, two-zone ground | 1 |\n| Per-apple `<visual>` emission + Label IDs + occlusion-targeted placement + visible-fraction validation loop | 2–3 |\n| LOD, perf tuning, RTF measurement, parameter profiles (세장방추형 / 키큰방추형) | 2–3 |\n| **Total** | **8–12 days** |\n\nThe original 1-week estimate was self-flagged low-confidence and is **optimistic**. Assume 8–12 days for a competent `bpy` user; 3× that for someone learning `bpy`. This is the single largest asset work item and everything downstream is blocked on it.\n\n---\n\n## 5. Disease imaging plan\n\n### 5.1 What the verdict changed (V5)\n\nThe original justification — \"rendered lesion texture lacks fine-scale appearance statistics\" — is **refuted as a mechanism** and must not appear in the writeup:\n\n- Klein et al. 2024 trained **exclusively on Blender renders** → **89.6%** on real greenhouse tomato photos. Purely-rendered training demonstrably *can* transfer. (Caveat: ~29-image real test set, post-adjustment figure; an existence proof against \"will not\", not proof it is easy.)\n- The same authors: \"photorealism (which is expensive to achieve) is **not the main quality driver**\". What fixed transfer was **content** — disease-pattern diversity, cluttered backgrounds, defocused branches, lighting variation.\n- Training on **real** PlantVillage photos scored \"barely better than random guessing\" on their real test set. Real texture statistics do not rescue transfer.\n- Noyan, arXiv:2206.04374: a model trained on **8 background pixels** of PlantVillage hit 49.0% vs 2.6% chance. Classifiers largely are not using lesion texture at all.\n- SDFormat 1.11 `<material><pbr><metal><albedo_map>` takes a **user-supplied image file**. Texture a leaf with a macro photo of real 갈색무늬병 and the fine-scale statistics *are* real photographic statistics. The alleged deficiency is an asset-authoring choice, not a Gazebo limit.\n\n**The genuine Gazebo-specific gaps are different:** no subsurface scattering for leaf translucency, rasterization not path tracing, no auto-exposure, no canopy sun-fleck dappling, no rolling shutter, no motion blur from skid-steer yaw jitter.\n\n### 5.2 What we will actually build\n\n**Split the two analytics targets — they have opposite sim-to-real profiles.**\n\n**(A) Fruit counting / yield — TRAIN IN SIM. This works.** Deep Count (Rahnemoonfar & Sheppard, *Sensors* 2017) trained on literally drawn coloured circles and hit **91.03%** on real tomato photos. Counting is low-spatial-frequency: blob count, colour, scale statistics. Use panoptic segmentation + 3D boxes for per-fruit instance IDs, and use sim to solve the actually-hard part — **cross-frame association / double-count elimination** — with perfect ground-truth tracks you can never obtain in the field.\n\n**(B) Disease — sim for pipeline, viewpoint planning and coverage; real data for the deployed model.** Not because renders look fake (V5), but because:\n\n1. **GSD physics.** 1920×1080 @ 60° h-FOV: frame width = 2·d·tan(30°).\n\n| Standoff | 1080p | 4K (3840×2160) | Min resolvable (≈3 px) @1080p |\n|---|---|---|---|\n| 1.0 m | **0.60 mm/px** | 0.30 mm/px | 1.8 mm |\n| 1.2 m | 0.72 mm/px | 0.36 mm/px | 2.2 mm |\n| 1.5 m | 0.90 mm/px | 0.45 mm/px | 2.7 mm |\n\nMarssonina lesions 5–10 mm = **8–17 px** ✅. Rust spots several mm ✅. Anthracnose fruit lesions 3–5 mm = 5–8 px ✅. **Acervuli (분생포자층) 0.1–0.2 mm = 0.17–0.33 px** ❌ — sub-pixel by ~5×, and 4K does not fix it. Yet Korean extension guidance names exactly this as the definitive marker: \"갈색무늬병을 구분할 수 있는 가장 뚜렷한 특징은 병반의 흑색돌기이며 ... 만져보면 거칠거칠합니다\". Resolving it needs ~0.05 mm/px ≈ 0.15 m standoff — an **arm / active-perception** problem, not a drive-by problem.\n\n2. **Biological ceiling.** Korean phytopathology states apple-blotch-like symptoms cannot be reliably separated from true *Marssonina coronaria* by eye; confirmation needs stereomicroscopy, culture, cross-section, PCR. No model removes this.\n\n3. **Domain shift binds regardless of data source.** Shibuya et al. (via arXiv:2510.12909): same-field macro F1 98.2–99.5% vs **cross-field 49.6–87.6%**, on all-real imagery, with background removal having \"only a limited impact.\"\n\n### 5.3 Scope tiers — say this in the proposal\n\n| Tier | Targets | Verdict |\n|---|---|---|\n| **1 — deliverable from the drive-by camera** | 붉은별무늬병 (*G. yamadae*, saturated orange-red, macro-scale, best signal-to-renderer ratio); 갈색무늬병 at **mid/late** severity (5–10 mm lesions + chlorosis + 조기낙엽); 점무늬낙엽병; 응애 damage as canopy-scale 황갈색 변색/조기낙엽; 사과혹진딧물 leaf curl **modelled as geometry deformation, not texture** | Build it |\n| **2 — needs stop-and-stare or a dedicated fruit camera** | 탄저병, 겹무늬썩음병 on fruit — only on unoccluded row-facing fruit | Stretch |\n| **3 — OUT OF SCOPE, and say why** | 부란병 (trunk cankers: different camera geometry entirely — low, downward, near-field, behind trellis and trunk guards); any early-stage diagnosis; anything hinging on acervuli texture | Cite the GSD table as justification. This is a strength of the writeup. |\n\n### 5.4 Rendering approach\n\n- **(a) Material swap — base layer, whole block.** Author ~6 leaf PBR sets (healthy, marssonina_mid, marssonina_late, alternaria, rust, mite_chlorosis) + 3 fruit sets, using **real macro photographs** as albedo maps (V5: this is legitimate, not a cheat). Assign per-tree from the generator. **Cost: 2–3 days. Yields tree-level labels only** — no per-lesion masks (see §6).\n- **(b) Lesion-as-geometry — 20–50 designated \"inspection trees\" only.** Each lesion = a small textured disc offset ~1 mm along the leaf normal, with its own Label plugin. **This is the only way to get per-lesion masks and boxes out of gz-sim.** Placement: Poisson-disc on leaf UV, size from the measured 3–10 mm distribution, vein-bounded for Marssonina. **Cost: 3–4 days.**\n- **(c) Diffusion style transfer — offline, optional.** ControlNet + LoRA structural conditioning (the VitiForge / 2026 mushroom-pipeline recipe). **Never bare CycleGAN** — documented failure: \"applying disease outside the leaf region into the background\", which silently invalidates your pixel-perfect masks. LeafGAN exists specifically to fix this with a segmentation constraint.\n- **Iterative refinement is mandatory, and refine CONTENT not photorealism** (V5). Klein et al.'s iteration 1 \"classified nearly all images as healthy\"; six refinement rounds were needed. Budget for that loop.\n\n### 5.5 Real-data anchor — start the applications today\n\n| Dataset | Content | Gate |\n|---|---|---|\n| **AI Hub 과수화상병 촬영 이미지** (`dataSetSn=146`) | ~145,938 apple images with bboxes, disease code, part, growth stage, **severity 0–3**, 진전도; covers 갈색무늬병 / 흑성병 / 점무늬낙엽병 / 탄저병 | **Korean nationals only, approval required** |\n| **AI Hub 과수원 내 로봇 주행 데이터 (사과, 배)** (`dataSetSn=71700`) | 253,851 apple orchard frames @1920×1080 + 138,504 `.pcd`, 2D bbox + semantic seg + 3D cuboid, collected 농로/노지 | Same gate. **Exactly your robot-POV geometry — this is the correct benchmark for the whole project, not just disease.** |\n| Plant Pathology 2021-FGVC8 (Kaggle) | ~18.6–23k in-field apple leaves, 6 classes | Open. US cultivars, **no 갈색무늬병 class** |\n| PlantDoc (CODS-COMAD 2020) | 2,598 in-field images | Open. **OOD test set only, never training** |\n| PlantVillage apple | 3,171 lab-isolated leaves | **Actively harmful.** 99% → 31% collapse cross-dataset |\n\n### 5.6 The honest statement, verbatim for the report\n\n> Simulation is used to develop and validate the autonomous data-collection pipeline, to optimize camera configuration, standoff and coverage, and to train and validate fruit counting and cross-frame fruit association. The disease model is trained on real field imagery (AI Hub 과수화상병 + Plant Pathology 2021 FGVC8) and evaluated on real Korean orchard robot-POV imagery (AI Hub 71700). Simulation-rendered disease imagery is used for pipeline verification, viewpoint-planning studies, and as auxiliary training data under iterative content refinement — not as the sole training source for the deployed model. The system output is framed as **symptom detection and severity mapping**, not species-level diagnosis: Korean phytopathology literature establishes that apple-blotch-like symptoms require microscopy/culture/PCR to separate from *Marssonina coronaria*, and the definitive visual marker (분생포자층, 0.1–0.2 mm) is sub-pixel at any standoff a ground robot can drive at (0.60 mm/px at 1.0 m, 1080p, 60° HFOV).\n\nNever report a sim-test disease accuracy without the real-data number beside it.\n\n---\n\n## 6. Ground-truth export\n\n**Four sensors, one link, identical pose and intrinsics, identical bridged `frame_id`.**\n\n```xml\n<plugin filename=\"gz-sim-sensors-system\" name=\"gz::sim::systems::Sensors\">\n  <render_engine>ogre2</render_engine>\n</plugin>\n\n<link name=\"cam_canopy_left_link\">\n  <sensor name=\"rgb\" type=\"camera\">\n    <topic>orchard/rgb</topic><update_rate>10</update_rate><always_on>1</always_on>\n    <camera><horizontal_fov>1.047</horizontal_fov>\n      <image><width>1920</width><height>1080</height><format>R8G8B8</format></image>\n      <clip><near>0.1</near><far>30</far></clip></camera>\n  </sensor>\n\n  <sensor name=\"semseg\" type=\"segmentation\">\n    <topic>orchard/semantic</topic><update_rate>2</update_rate><always_on>1</always_on>\n    <camera><segmentation_type>semantic</segmentation_type>\n      <horizontal_fov>1.047</horizontal_fov>\n      <image><width>1920</width><height>1080</height></image>\n      <clip><near>0.1</near><far>30</far></clip></camera>\n  </sensor>\n\n  <!-- VERDICT: \"panoptic\" and \"instance\" are ALIASES for ONE mode, not two.\n       gz-sim8 examples/worlds/segmentation_camera.sdf uses <segmentation_type>instance\n       for the sensor it names \"panoptic\". Do not budget for three modes. -->\n  <sensor name=\"panoptic\" type=\"segmentation\">\n    <topic>orchard/panoptic</topic><update_rate>2</update_rate><always_on>1</always_on>\n    <camera><segmentation_type>panoptic</segmentation_type>\n      <horizontal_fov>1.047</horizontal_fov>\n      <image><width>1920</width><height>1080</height></image>\n      <clip><near>0.1</near><far>30</far></clip></camera>\n  </sensor>\n\n  <sensor name=\"boxes_vis\" type=\"boundingbox_camera\">\n    <topic>orchard/boxes_visible_2d</topic><update_rate>2</update_rate><always_on>1</always_on>\n    <camera><box_type>visible_2d</box_type>   <!-- occlusion-aware -->\n      <horizontal_fov>1.047</horizontal_fov>\n      <image><width>1920</width><height>1080</height></image>\n      <clip><near>0.1</near><far>30</far></clip></camera>\n  </sensor>\n\n  <sensor name=\"boxes_full\" type=\"boundingbox_camera\">\n    <topic>orchard/boxes_full_2d</topic><update_rate>2</update_rate><always_on>1</always_on>\n    <camera><box_type>full_2d</box_type>      <!-- amodal -->\n      <horizontal_fov>1.047</horizontal_fov>\n      <image><width>1920</width><height>1080</height></image>\n      <clip><near>0.1</near><far>30</far></clip></camera>\n  </sensor>\n</link>\n```\n\n`visible_2d ÷ full_2d` per fruit **is** the occlusion measurement. That single ratio is how you verify your generated orchard lands in the published 40.85–79.83% band. Do not skip the second bounding-box sensor.\n\n**Labels — opt-in, per-visual, baked into the world SDF:**\n\n```xml\n<include>\n  <name>tree_r03_t12</name>\n  <uri>model://apple_tallspindle_marssonina</uri>\n  <plugin filename=\"gz-sim-label-system\" name=\"gz::sim::systems::Label\">\n    <label>21</label>\n  </plugin>\n</include>\n```\n…and inside the model, each apple `<visual>` and each lesion decal `<visual>` carries its own Label plugin.\n\n**Label ID plan (hard limit 0–255 semantic; `labelColor = label/255.0f` in `Ogre2SegmentationMaterialSwitcher.cc`):**\n\n`0` background · `10` ground · `11` weed · `12` trellis_post · `13` wire · `20` trunk · `21` branch · `30` leaf_healthy · `31` leaf_marssonina · `32` leaf_alternaria · `33` leaf_rust · `34` leaf_mite_chlorosis · `35` leaf_aphid_curl · `40` fruit_healthy · `41` fruit_anthracnose · `42` fruit_whiterot · `50–59` lesion_* decals\n\n**Resulting gz topics:** `orchard/semantic/labels_map`, `.../colored_map`, `.../camera_info`; `orchard/panoptic/labels_map`, `.../colored_map`; `orchard/boxes_visible_2d` (`AnnotatedAxisAligned2DBox_V`), `orchard/boxes_visible_2d_image`, `.../camera_info`.\n\n**`ros_gz_bridge` YAML** (ros_gz jazzy 1.0.23; `vision_msgs` mappings landed in 1.0.0, 2024-04-24, predating Jazzy's 2024-05-23 release):\n\n```yaml\n- {ros_topic_name: /orchard/rgb, gz_topic_name: /orchard/rgb,\n   ros_type_name: sensor_msgs/msg/Image, gz_type_name: gz.msgs.Image, direction: GZ_TO_ROS}\n- {ros_topic_name: /orchard/semantic/labels_map, gz_topic_name: /orchard/semantic/labels_map,\n   ros_type_name: sensor_msgs/msg/Image, gz_type_name: gz.msgs.Image, direction: GZ_TO_ROS}\n- {ros_topic_name: /orchard/panoptic/labels_map, gz_topic_name: /orchard/panoptic/labels_map,\n   ros_type_name: sensor_msgs/msg/Image, gz_type_name: gz.msgs.Image, direction: GZ_TO_ROS}\n- {ros_topic_name: /orchard/boxes_visible_2d, gz_topic_name: /orchard/boxes_visible_2d,\n   ros_type_name: vision_msgs/msg/Detection2DArray,\n   gz_type_name: gz.msgs.AnnotatedAxisAligned2DBox_V, direction: GZ_TO_ROS}\n- {ros_topic_name: /orchard/boxes_full_2d, gz_topic_name: /orchard/boxes_full_2d,\n   ros_type_name: vision_msgs/msg/Detection2DArray,\n   gz_type_name: gz.msgs.AnnotatedAxisAligned2DBox_V, direction: GZ_TO_ROS}\n```\nUse `override_frame_id` so all five share the RGB optical frame.\n\n**Six non-negotiable rules, all verdict-sourced:**\n\n1. **Texture-painted lesions produce ZERO masks and ZERO boxes.** Segmentation flat-fills an entire *visual* with one label colour; there is no UV/texture-space label channel anywhere in the pipeline. **Prototype lesion-as-separate-visual on ONE tree in week 1**, before building the orchard.\n2. **Emit a FLAT world.** gz-sim issue **#1579** (open, filed 2022-07-07): nested labeled `<include>`s make panoptic `labels_map` return zeros and give identical colours to distinct copies. Never wrap rows in parent models containing labeled includes.\n3. **Never spawn labeled models at runtime** — they silently lose their labels. Bake all variation into pre-generated world files.\n4. **`labels_map` is `RGB_INT8`.** Any `image_transport` compressed republish, lossy rosbag2 compression, or JPEG write corrupts integer labels into plausible-but-wrong classes. Raw PNG only.\n5. **Instance IDs are 16-bit (65,535) per label.** Fine for per-fruit within a block; will overflow if you try per-leaf across the orchard. Restrict per-instance labeling to fruit and to inspection-tree lesions.\n6. **Timestamp alignment.** RGB at 10 Hz and GT at 2 Hz will not share timestamps. Prefer `<triggered>`/`<trigger_topic>` so all five capture the same instant — **verify per-sensor-type trigger support in gz-sim 8 first**; if unsupported for `segmentation`/`boundingbox_camera`, match on exact sim time and **assert timestamp equality in the dataset writer**. Silently mismatched RGB/mask pairs are the classic way a synthetic dataset becomes quietly worthless.\n\nDocs note: `https://gazebosim.org/api/sensors/8/boundingbox_camera.html` **returns HTTP 404**. The sensor is present in Harmonic (`BoundingBoxCameraSensor` in gz-sensors 8.2.2; `gz-sim8/examples/worlds/boundingbox_camera.sdf`), but you must read the **v7 or v9** tutorial for the v8 API.\n\n---\n\n## 7. What this adds to build effort vs teleop-only\n\n**Teleop-only baseline (drive with a joystick, record images):**\n\n| Item | Days |\n|---|---|\n| Secure Boot / MOK / DKMS fix + GL vendor verification | 0.5–1 |\n| Scout Mini URDF + gz Harmonic bringup + `/clock` + TF | 2–3 |\n| Crude orchard world (v0 generator, no leaves, no labels) | 1–2 |\n| Teleop + naive image capture | 1 |\n| **Baseline total** | **≈ 5–7 days** |\n\n**Incremental cost of the full autonomous + labeled-analytics plan:**\n\n| Work item | Days |\n|---|---|\n| 360° nav LiDAR SDF + `pointcloud_to_laserscan` + STVL + costmap tuning | 2 |\n| `robot_localization` dual-EKF + `navsat_transform` + **custom canopy-GNSS degradation node** (fix→float transitions, correlated multipath — without this you validate nothing) | 3 |\n| Ground-truth `map→odom` scaffold node | 0.5 |\n| Nav2 bringup, BT wiring, MPPI + RotationShim tuning in a 2.0 m corridor | 4 |\n| `nav2_route` graph, typed row/headland edges, per-edge planner+controller selection | 3 |\n| `RowCenterlinePlanner` `nav2_core::GlobalPlanner` plugin (~300 LOC C++) | 4 |\n| Row-centreline Y/yaw estimator + end-of-row detector + discrete X reset | 3 |\n| **Full procedural orchard generator** (leaves, trellis, per-apple visuals, Label IDs, occlusion-targeted placement, LOD) | **8–12** |\n| Disease PBR material sets (6 leaf + 3 fruit) + lesion-decal geometry on inspection trees | 5 |\n| GT sensor rig + bridge + dataset writer + timestamp assertions + label-integrity tests | 3 |\n| Occlusion budget + camera-placement sweep experiments | 3 |\n| Performance/LOD/RTF work | 3 |\n| AI Hub application, download, ingestion, real-data training/eval harness | 4 |\n| **Incremental total** | **≈ 45–49 days** |\n\n**Grand total ≈ 50–56 person-days (10–11 weeks solo) vs 5–7 days teleop — roughly 8×.**\n\nComposition of the delta: **~30% orchard asset + labeling pipeline**, ~30% Nav2 + custom plugins, ~15% localization, ~15% ground-truth/dataset infrastructure, ~10% performance. **The single largest line item is the asset pipeline, not Nav2.** If schedule pressure hits, the correct cut is *not* Nav2 — it is scene scale (4×20 instead of 6×30) and lesion-geometry coverage (10 inspection trees instead of 50).\n\n---\n\n## 8. Open questions for the user (ranked)\n\n**Q1 — Is there a real-hardware deliverable, or is this sim-only? (blocks the BOM and ~5 days)**\nThe MID-70 has **no ROS 2 Jazzy driver** (`livox_ros_driver2` supports HAP + Mid360 only; `livox_ros2_driver` is v0.0.1-beta on Dashing/Foxy/Humble) **and no built-in IMU**. Options: **(a) sim-only** — the gap is irrelevant, `gpu_lidar` publishes `PointCloud2` natively; **(b)** swap to a MID-360 on hardware, keeping MID-70 in sim only; **(c)** port `livox_ros2_driver` to Jazzy (~3–5 days, then you own it forever).\n**→ Default: (a). Declare sim-only now; revisit at hardware procurement.**\n\n**Q2 — Is a second, 360° LiDAR acceptable in the sensor set?**\nThis is the load-bearing question of §1. Options: **(a)** virtual 360° `gpu_lidar` in sim + **Livox MID-360** on hardware (~USD 650–900; same vendor, one driver, Jazzy-supported); **(b)** virtual 360° in sim + a cheap 2D scanner (LD19 ~USD 100 / RPLIDAR S2) on hardware; **(c)** MID-70 only — accept no lateral/rear costmap coverage, no raytrace clearing, and phantom-obstacle traps during in-place rotation.\n**→ Default: (a).** (c) is a research question, not a plan; if the user wants it, build (a) first as the regression baseline and *then* measure what (c) loses.\n\n**Q3 — Will you apply for AI Hub datasets 146 and 71700 now?**\nBoth are **Korean-nationals-only with an approval step**, and both are on the critical path for the disease target with no substitute. Options: **(a)** apply this week for both; **(b)** apply for 146 only; **(c)** fall back to Plant Pathology 2021-FGVC8 + PlantDoc (US cultivars, **no 갈색무늬병 class**, 2,598 OOD images).\n**→ Default: (a), today.** Approval latency is schedule risk that costs nothing to start eliminating. 71700 in particular is the correct benchmark for the *entire* project, not just disease.\n\n**Q4 — Per-apple instance labeling: full block or targeted?**\n10,800 apple `<visual>`s is the main draw-call risk on the iGPU. Options: **(a)** per-apple visuals on all 180 trees; **(b)** per-apple visuals on the 2 central rows + 20–50 inspection trees, background rows get one merged fruit submesh; **(c)** per-apple only on a single 20-tree row.\n**→ Default: (b).** Measure RTF on one row before committing either way — and treat the measurement as a gate, not a formality.\n\n---\n\n## 9. Risks, ranked by what bites first\n\n1. **Broken NVIDIA driver — correctness, not just performance.** Gazebo's own troubleshooting page states that defaulting to Intel graphics on a hybrid machine produces **\"incorrect shadows or laser scans.\"** Both your LiDARs are `gpu_lidar` rendering sensors. You could burn weeks debugging a Nav2 failure that is a rendering-backend artifact. Fix via `mokutil --import` (enroll the DKMS key; do not disable Secure Boot), verify with `glxinfo | grep vendor`, and set `QT_QPA_PLATFORM=xcb` for the Wayland session (Ogre+Qt have no native Wayland support). Also watch for `llvmpipe` fallback → black 3D areas (gz-sim #1116). **Do this before anything else.**\n2. **Segmentation flat-fills visuals.** Texture-painted lesions yield zero masks and zero boxes. If the whole disease pipeline is designed around material swapping and this is discovered after the orchard is built, the entire ground-truth story is lost. **Prototype lesion-as-visual on one tree in week 1.**\n3. **Draw-call explosion.** 10,800 apple visuals + alpha-tested leaf cards (which defeat early-Z) + five simultaneous rendering sensors, on an iGPU. Sub-1.0 RTF silently decouples Nav2's control loop from simulated sensor rates, producing controller tuning that is invalid on hardware. Mitigations: `<shadows>0</shadows>`, one directional light, primitive collision shapes only (skip collision on apples and leaves entirely), 80-tri apples not 320, `Ogre2Visual` static flag, GT cameras at 1–2 Hz or triggered. **Measure one row before committing.**\n4. **Asset pipeline on the critical path.** 8–12 days, everything downstream blocked, no download shortcuts it, and the estimate has low confidence.\n5. **Along-row (X) unobservability.** Affects AMCL, slam_toolbox loop closure and FAST-LIO2 PGO **identically** — swapping localizers does not make it go away. Mitigated by design (dead-reckon X, reset at row-end events), but if end-of-row detection is unreliable the mitigation collapses.\n6. **gz-sim #1579 + runtime-spawn label loss.** An orchard naturally wants row→tree→fruit nesting; that is precisely the pattern that silently produces garbage instance IDs. Flat world SDF, no runtime spawning.\n7. **Tighter corridor than originally planned (V6).** Free alley 2.0–2.3 m, not 2.95 m. `inflation_radius` above ~0.55 m makes the two canopy walls' inflation fields meet and renders the whole row high-cost. Symptom presents as \"the planner refuses to enter rows\" or \"the controller oscillates\" — reads as a planner bug, is a costmap parameter. **Visually inspect the costmap in RViz before debugging anything upstream.** Compounded by MPPI's documented narrow-corridor jitter if `repulsion_weight` is high relative to `inflation_radius`.\n8. **Simulated GNSS lies.** gz NavSat models position + optional Gaussian noise. No multipath, no canopy attenuation, no fix→float state machine, no correlated bias. Real leaf-on dual-band RTK degrades to 0.17–0.18 m (vs 0.07 leaf-off); single-band to 1.5–3.0 m with >5 m maxima. Your analytics window is **leaf-on** — the worst case. A GNSS-dependent design validated in Gazebo is not validated.\n9. **Simulated MID-70 lies in the opposite direction.** `livox_laser_simulation` is Gazebo Classic only; Harmonic forces a uniform ray grid, removing exactly the angular sparsity and temporal irregularity that make a rosette hard to consume. Match the 10,000 pts/frame budget and consider decimating/time-jittering rays.\n10. **Partial `use_sim_time`.** The single most common Nav2-in-sim failure, and it presents as erratic controller behaviour, not as a clock error. Every Nav2 lifecycle node, `robot_state_publisher`, both EKFs and every custom node.\n11. **AI Hub approval latency or denial.** No substitute exists for Korean apple disease + robot-POV data. Fallback (FGVC8 + PlantDoc) has no 갈색무늬병 class.\n12. **Over-claiming.** It is trivially easy to report 95%+ disease accuracy on a held-out *simulated* test set. That number measures how well the model memorised your own texture set. Never report it without the real-data number beside it.\n13. **Non-rigid foliage.** Wind-moved leaves are pervasive dynamic outliers to both scan matching and the costmap. STVL `voxel_decay` becomes a trade-off between forgetting real trunks and accumulating leaf noise — and Gazebo trees do not move in wind unless you make them, so sim will not predict the tuning.\n14. **`labels_map` corruption via any lossy path**, and RGB/mask timestamp mismatch. Both fail silently and both poison the dataset rather than crashing.\n\n---\n\n### Key references\nLivox MID-70 spec: livoxtech.com/mid-70/specs · `Livox Mid-70 User Manual EN v1.2` · Nav2: index.ros.org/p/nav2_mppi_controller (Jazzy 1.3.12), `navigation2/jazzy/nav2_smac_planner/README.md`, `nav2_mppi_controller/README.md`, docs.nav2.org/tuning · STVL: index.ros.org/p/spatio_temporal_voxel_layer (Jazzy 2.5.5) · gz-sim8 `examples/worlds/segmentation_camera.sdf`, `boundingbox_camera.sdf`; `Ogre2SegmentationMaterialSwitcher.cc`; gz-sim issue #1579, #1116 · gazebosim.org/docs/latest/troubleshooting · gazebosim.org/api/sim/9/model_and_optimize_meshes.html · ros_gz jazzy `ros_gz_bridge/README.md` · RDA 농사로 cntntsNo=30663 · Robinson et al., *NY Fruit Quarterly* 14(2), 2006 · Roy/Isler arXiv:1808.04336 · *Machines* 11(1):84 · Klein et al. doi:10.3389/fpls.2024.1360113 · Noyan arXiv:2206.04374 · Fei & Vougioukas arXiv:2107.01321 · arXiv:2407.18535 · arXiv:2603.23112 · github.com/FieldRobotEvent/virtual_maize_field (GPL-3.0) · github.com/tduboudi/IAMPS2019-Procedural-Fruit-Tree-Rendering-Framework (MIT) · ambientcg.com, polyhaven.com (CC0) · aihub.or.kr `dataSetSn=146`, `dataSetSn=71700`",
    "dimensions": [
      "nav2",
      "apple",
      "disease"
    ],
    "verdicts": [
      {
        "claim": "A Livox MID-70 has a circular field of view of about 70.4 degrees and is therefore unsuitable on its own as the sole sensor for Nav2 AMCL localization and a 360-degree obstacle costmap; a practical orchard Nav2 setup needs either an additional wider-FOV sensor, a persistent/voxel costmap layer, or GNSS+IMU+odometry fusion.",
        "verdict": "PARTIALLY_TRUE",
        "correction": "The hardware number is exactly right and the bottom-line conclusion (\"the Mid-70 alone is not enough\") survives, but the stated MECHANISM is wrong in two specific, checkable ways, and one of the three proposed remedies is not a remedy at all.\n\nWHAT IS CONFIRMED\n- \"70.4 degrees circular\" is verbatim correct. Livox's official spec page and User Manual v1.2 both print \"FOV: 70.4° (circular)\". Not approximate — that is the published figure.\n- The practical conclusion is correct: a Mid-70 cannot be the sole sensor for a full Nav2 stack in an orchard.\n\nERROR 1 — AMCL has no 360-degree requirement, so \"therefore\" does not follow.\nThe claim implies the 70.4° FOV is what disqualifies AMCL. It is not. nav2_amcl computes beam angles as angle_min + (i * angle_increment) wrapped to [-pi, pi] and adapts to whatever angular range the message advertises. There is no hardcoded full-circle check. Narrow FOV degrades AMCL statistically (fewer independent beams, weak particle discrimination, and in a self-similar tall-spindle orchard, near-total ambiguity ALONG the row axis) — but that is a performance argument, not a disqualifying one.\nThe actual hard blockers are different and stronger:\n  (a) Message type. nav2_amcl subscribes ONLY to sensor_msgs/msg/LaserScan. Livox drivers publish Livox PointCloud2 (PointXYZRTL/RTLT), Livox CustomMsg, or PCL PointXYZI — never LaserScan. A pointcloud_to_laserscan stage is mandatory regardless of FOV.\n  (b) Scan geometry. The Mid-70 is non-repetitive rosette. Beams neither lie in a plane nor repeat between frames. Flattening that into a fixed-geometry 2D LaserScan violates AMCL's beam-model assumptions far more fundamentally than the 70.4° aperture does. This is the real reason, and the claim never mentions it.\n  (c) AMCL also needs a pre-built static occupancy grid — a separate problem in an open-field (노지) orchard.\n\nERROR 2 — \"a persistent/voxel costmap layer\" is already the default, not a fix.\nThere is no \"persistent layer\" plugin in Nav2. More importantly, nav2_costmap_2d's VoxelLayer/ObstacleLayer ALREADY persist marks by default: marking writes LETHAL_OBSTACLE, and clearing happens only via raytraceFreespace() along rays inside the current sensor FOV. Cells outside the FOV retain prior markings. So a narrow-FOV sensor already accumulates a surround-view costmap as the robot drives.\nThis inverts the claim's failure mode. The problem is not that obstacles fail to persist — it is that they persist TOO well: with a 70.4° forward cone the robot can never re-observe cells beside or behind it, so it cannot raytrace stale marks away. Expect phantom obstacles trapping a skid-steer SCOUT MINI during in-place rotation. Adding a voxel layer makes this worse, not better. It is also irrelevant to localization.\n\nERROR 3 — The three remedies are not interchangeable alternatives.\nPresented as \"either/or\", but localization and obstacle avoidance are separate problems. GNSS+IMU+odometry fusion solves localization only; a wider-FOV sensor solves obstacle coverage only. You need one from each column, not one from three. GNSS+IMU+odom IS officially supported by Nav2 (robot_localization + navsat_transform_node + GPS waypoint following, per the official docs.nav2.org GPS tutorial) — but note GNSS multipath/attenuation under apple canopy degrades RTK fix even in tall-spindle rows, so it is not a clean substitute either.\n\nOMISSION THAT MATTERS MORE THAN THE CLAIM ITSELF\nFor this specific project (ROS 2 Jazzy), the Mid-70 has no official ROS 2 driver path. livox_ros_driver2 README lists only HAP and Mid360. livox_ros_driver (v1) targets ROS1 indigo/kinetic/melodic on Ubuntu 14.04/16.04/18.04. livox_ros2_driver is v0.0.1-beta targeting Dashing/Foxy/Humble — Jazzy is not mentioned. This is a far more immediate blocker than field of view.\n\nCORRECTED STATEMENT\n\"The Livox Mid-70 has a 70.4° circular FOV with a non-repetitive rosette scan and publishes only point-cloud messages. It cannot drive Nav2 AMCL directly — AMCL consumes sensor_msgs/LaserScan only, and a non-repetitive, non-planar pattern does not flatten into a stable 2D scan. Nav2's obstacle/voxel layers already persist marks outside the sensor FOV, so the costmap risk is stale un-clearable obstacles rather than missing ones. A workable orchard stack therefore needs a localization source independent of AMCL (GNSS+IMU+wheel-odometry fusion via robot_localization, and/or LiDAR-inertial odometry such as FAST-LIO/Point-LIO which suit the Mid-70's scan pattern) AND separate wider-FOV obstacle coverage (depth cameras or a 360° 2D scanner), plus a resolved ROS 2 Jazzy driver story for the Mid-70.\"",
        "evidence": "PRIMARY SOURCES CONSULTED\n\n1. Livox official spec page — https://www.livoxtech.com/mid-70/specs\n   \"Field of View: 70.4° (Circular)\". Range 90 m @10%, 130 m @20%, 260 m @80% reflectivity @100 klx. Point rate 100,000 pts/s (first/strongest return), 200,000 pts/s (dual return). Range precision <=2 cm @20 m, <=3 cm @0.2-1 m. Angular accuracy <0.1°. 905 nm. 580 g. 8 W avg, 10-15 V DC.\n\n2. Livox Mid-70 User Manual EN v1.2 (2021.02), official DJI CDN PDF — https://terra-1-g.djicdn.com/65c028cd298f4669a7f0e40e50ba1131/Download/Mid-70/new/Livox%20Mid-70%20User%20Manual_EN_v1.2.pdf (extracted with pdftotext)\n   - \"FOV / 70.4° (circular)\" (spec table, appears twice)\n   - \"Wide FOV: Compared with Mid-40, the Mid-70 has a larger circular field of view (FOV) of 70.4°\"\n   - \"Close Proximity Blind Zone / 0.05 m\"; \"The blind zone of the Mid-70 is 5 cm, and the detection precision from 0.2 to 1 m is 3 cm.\"\n   - \"Non-Repetitive Scanning Technology: Livox's unique non-repetitive scanning technology ensures a high-density point cloud\"\n   - \"After 1.5 seconds, the FOV coverage approaches 86%\" — confirms coverage is an INTEGRATION-TIME property, i.e. instantaneous single-frame coverage is sparse. Relevant to why a per-frame 2D flattening is unstable.\n   - \"the effective detection range of the Livox Mid-70 varies based on where the object is within the FOV. The closer to the edge of the FOV, the shorter the effective detection range\" — the usable aperture is effectively narrower than 70.4° at range.\n   - Interface: Ethernet / UDP via Livox Converter 2.0.\n\n3. nav2_amcl source, jazzy branch — https://raw.githubusercontent.com/ros-navigation/navigation2/jazzy/nav2_amcl/src/amcl_node.cpp\n   Subscription is LaserScan-only:\n     laser_scan_sub_ = std::make_unique<message_filters::Subscriber<sensor_msgs::msg::LaserScan, rclcpp_lifecycle::LifecycleNode>>(shared_from_this(), scan_topic_, rmw_qos_profile_sensor_data, sub_opt);\n   Beam angles computed as \"angle_min + (i * angle_increment)\", wrapped to [-pi, pi]. No hardcoded 360° requirement — the code adapts to any angular range. This REFUTES the claim's implied mechanism.\n\n4. nav2_costmap_2d VoxelLayer source, jazzy branch — https://raw.githubusercontent.com/ros-navigation/navigation2/jazzy/nav2_costmap_2d/plugins/voxel_layer.cpp\n   Observations consumed as PointCloud2 (sensor_msgs::PointCloud2ConstIterator<float> iter_x(cloud, \"x\")). Marking in updateBounds() via voxel_grid_.markVoxelInMap(mx, my, mz, mark_threshold_) -> LETHAL_OBSTACLE, with min/max_obstacle_height filtering. Clearing ONLY via raytraceFreespace() / clearVoxelLineInMap() along rays from sensor origin, i.e. only within the current sensor FOV. Cells outside the FOV retain prior markings. This REFUTES \"needs a persistent layer\" — persistence is the default, and it is the stale-obstacle direction that bites.\n\n5. Nav2 official GPS tutorial — https://docs.nav2.org/tutorials/docs/navigation2_with_gps.html\n   Describes \"a localization system using a GPS sensor(s) as the source of global positioning, robot_localization (RL) for sensor fusion, and how to use Nav2 to follow GPS waypoints.\" Confirms GNSS+IMU+odom fusion is an officially supported, AMCL-independent global positioning path. This part of the claim is SUPPORTED.\n\n6. Livox driver repos (ROS 2 Jazzy availability for Mid-70)\n   - https://raw.githubusercontent.com/Livox-SDK/livox_ros_driver2/master/README.md — supported devices listed as \"HAP\" and \"Mid360\", \"(more types are comming soon...)\". Mid-70 NOT listed. Publishes xfer_format 0=Livox PointCloud2 (PointXYZRTLT), 1=Livox custom, 2=PCL PointXYZI. No sensor_msgs/LaserScan.\n   - https://raw.githubusercontent.com/Livox-SDK/livox_ros_driver/master/README.md — \"ubuntu 14.04/16.04/18.04 ... ROS environment (indigo, kinetic, melodic)\". ROS1 only.\n   - https://raw.githubusercontent.com/Livox-SDK/livox_ros2_driver/master/README.md — \"v0.0.1-beta\", build instructions for Dashing/Foxy/Humble. Jazzy not mentioned.\n   Across all three official drivers: none publishes LaserScan, and none documents Mid-70 on ROS 2 Jazzy.\n\nMETHOD NOTE\nWebSearch budget was exhausted mid-task (200/200); all findings above were obtained via direct WebFetch of primary sources (vendor spec page, vendor PDF manual, upstream GitHub source files, official Nav2 docs), which is the stronger evidence class anyway. No peer-reviewed orchard-navigation papers were retrieved, so the specific quantitative claim \"AMCL degrades unacceptably at 70.4° in orchard rows\" remains UNVERIFIED by literature here — it is argued from AMCL's source-level beam model and orchard self-similarity, not measured."
      },
      {
        "claim": "Gazebo Harmonic (gz-sim 8) ships a segmentation camera sensor supporting both semantic and panoptic/instance modes, and a bounding-box camera sensor supporting 2D and 3D boxes, both bridgeable to ROS 2.",
        "verdict": "CONFIRMED",
        "correction": "The claim is accurate as stated, but four refinements matter before designing around it:\n\n1. \"panoptic/instance\" is ONE mode, not two. `<segmentation_type>` has exactly two behaviors: `semantic` (default) and panoptic — where the strings `panoptic` and `instance` are aliases for the same thing. gz-sim8's own example world uses `instance` for the sensor it names \"panoptic\". Do not budget for three modes.\n\n2. The bounding box camera is undocumented for Harmonic specifically. The gz-sensors 8 tutorial index (gazebosim.org/api/sensors/8/tutorials.html) lists only Introduction, Installation, Custom sensors, Thermal Camera and Segmentation Camera — there is NO bounding-box tutorial page under /api/sensors/8/, and /api/sensors/8/boundingbox_camera.html returns HTTP 404. The tutorial exists only under /api/sensors/7/, /api/sensors/9/ and /api/rendering/8/. The sensor itself is genuinely present in Harmonic (class BoundingBoxCameraSensor in gz-sensors 8.2.2; example world in the gz-sim8 branch), so \"ships\" is true — but you must read the v7 or v9 tutorial for the v8 API.\n\n3. Known open upstream bug affecting exactly this project's world structure: gz-sim issue #1579 (open since 2022-07-07) reports that in PANOPTIC mode, when labeled models are reached through nested `<include>` tags, `/panoptic/labels_map` returns zeros and `/panoptic/colored_map` gives identical colors to distinct copies of the same object. Semantic mode is unaffected. An orchard built the natural way — a \"row\" model that nests tree includes, each nesting apple includes — is precisely the nesting pattern that triggers this. Validate instance IDs early or keep labeled models flat at world level.\n\n4. Labels are entirely manual and opt-in. Only models carrying the `gz-sim-label-system` plugin (`gz::sim::systems::Label`) with a numeric `<label>` are visible to either sensor; everything unlabeled becomes background. Per-apple instance IDs for fruit counting therefore require attaching a label plugin to every individual fruit visual/model, not just to the tree.\n\nAdditionally, out of scope of the claim but decisive for the target machine: both sensors are gz-rendering ogre2 GPU paths and are among the heaviest in the stack. With the GTX 1660 SUPER's DKMS module blocked by Secure Boot, these would fall back to the Alder Lake iGPU or llvmpipe, where segmentation and bounding-box cameras will be prohibitively slow. Fix the driver before relying on this pipeline.",
        "evidence": "SEGMENTATION CAMERA (confirmed in Harmonic):\n- Primary doc: https://gazebosim.org/api/sensors/8/segmentationcamera_igngazebo.html — `<segmentation_type>` tag accepts `semantic` (default), `panoptic`, or `instance`. Semantic: each pixel carries the class label, same-label objects share a color. Panoptic: each pixel carries label + instance count (label ID in the third channel, instance count as a 16-bit value across two channels). Publishes two `gz::msgs::Image` topics: `<topic>/labels_map` and `<topic>/colored_map`. States \"Gazebo Fortress or newer\" as the minimum. Requires `gz-sim-label-system` on `<visual>`, `<model>` or `<include>`; unlabeled models are background.\n- Code/example, gz-sim8 branch (Harmonic): examples/worlds/segmentation_camera.sdf defines `<sensor type=\"segmentation\">` twice, with `<segmentation_type>instance</segmentation_type>` on topic `panoptic` and `<segmentation_type>semantic</segmentation_type>` on topic `semantic`. Label plugin filename `gz-sim-label-system`, name `gz::sim::systems::Label`; cars=40, trees=30, cones=20, house unlabeled (background).\n\nBOUNDING BOX CAMERA (confirmed in Harmonic):\n- Class exists in gz-sensors 8.2.2: https://gazebosim.org/api/sensors/8/classgz_1_1sensors_1_1BoundingBoxCameraSensor.html — \"creates a BoundingBox image from an gz rendering scene... offers both an gz-transport interface and a direct C++ API\".\n- Code/example, gz-sim8 branch: examples/worlds/boundingbox_camera.sdf defines three sensors of `type=\"boundingbox_camera\"` with `<box_type>full_2d</box_type>` (topic boxes_full_2d), `<box_type>visible_2d</box_type>` (topic boxes_visible_2d), and `<box_type>3d</box_type>` (topic boxes_3d). This is direct proof both 2D and 3D ship in Harmonic.\n- Tutorial (v9/v7 text, same API): https://gazebosim.org/api/sensors/9/boundingbox_camera.html — box_type accepts `2d`/`visible_2d`/`visible_box_2d`, `full_2d`/`full_box_2d`, and `3d`; default is visible 2D. 2D modes publish `gz::msgs::AnnotatedAxisAligned2DBox_V`; 3D publishes `gz::msgs::AnnotatedOriented3DBox_V`; 2D additionally publishes a drawn-boxes image on `<TOPIC>_image`. Minimum \"Gazebo Fortress or newer\".\n\nROS 2 BRIDGING (confirmed for the Jazzy release specifically, not just rolling):\n- https://raw.githubusercontent.com/gazebosim/ros_gz/jazzy/ros_gz_bridge/package.xml — ros_gz_bridge version 1.0.23, and `<depend>vision_msgs</depend>` is present.\n- https://raw.githubusercontent.com/gazebosim/ros_gz/jazzy/ros_gz_bridge/README.md mapping table rows: `vision_msgs/msg/Detection2D` | `gz.msgs.AnnotatedAxisAligned2DBox`; `vision_msgs/msg/Detection2DArray` | `gz.msgs.AnnotatedAxisAligned2DBox_V`; `vision_msgs/msg/Detection3D` | `gz::msgs::AnnotatedOriented3DBox`; `vision_msgs/msg/Detection3DArray` | `gz::msgs::AnnotatedOriented3DBox_V`.\n- Provenance from ros_gz_bridge CHANGELOG.rst: 2D support landed in 0.246.0 (2023-08-31), \"Added messages for 2D Bounding Boxes to ros_gz_bridge (#458) (#466)\"; 3D support landed in 1.0.0 (2024-04-24), \"Added conversion for Detection3D and Detection3DArray (#523) (#525)\". ros_gz 1.0.0 (2024-04-24) predates the Jazzy release (2024-05-23), so both are in Jazzy from day one.\n- Segmentation output needs no special mapping: both labels_map and colored_map are plain `gz::msgs::Image`, bridged to `sensor_msgs/msg/Image` by the long-standing core mapping.\n\nCONTRARY / CAVEAT EVIDENCE:\n- https://gazebosim.org/api/sensors/8/tutorials.html lists no bounding-box tutorial; https://gazebosim.org/api/sensors/8/boundingbox_camera.html returns HTTP 404 (docs gap only — code confirmed present).\n- https://github.com/gazebosim/gz-sim/issues/1579 — OPEN, filed 2022-07-07, \"Unexpected Behavior for Segmentation Camera in Panoptic Mode With Nested Includes\": labels_map shows zeros and colored_map gives duplicate colors to copies when labeled models sit inside nested includes; semantic mode unaffected. Reported against Fortress 6.9.0 / libignition-sensors6-segmentation-camera 6.3.0, never closed, so it must be assumed live in gz-sensors 8."
      },
      {
        "claim": "A plant-disease classifier trained purely on Gazebo-rendered synthetic leaf imagery will not transfer reliably to real orchard photographs, because rendered lesion texture lacks the fine-scale appearance statistics of real infections.",
        "verdict": "PARTIALLY_TRUE",
        "correction": "The warning is directionally sound but the claim is overstated in its conclusion and its stated causal mechanism is refuted by primary sources.\n\nWHAT SURVIVES: A sim-to-real gap is real and large, and a naive first attempt will fail. In Klein et al. (2024), the first iteration of a purely-synthetic-trained classifier \"classified nearly all images as healthy\" — total failure to generalize. Six refinement iterations were needed. So \"point Gazebo at a leaf mesh, train, deploy\" will not work.\n\nREFUTED PART 1 — \"will not transfer reliably\" as a categorical statement. Klein, Waller, Pirk, Palubicki, Tester & Michels (Frontiers in Plant Science 15, 16 Sept 2024, doi:10.3389/fpls.2024.1360113) trained a classifier \"by exclusively using synthetic images\" (Blender-rendered, zero real training images) and reached 89.6% accuracy on real greenhouse tomato leaf photographs. Purely-rendered training demonstrably CAN transfer. Caveat I must flag: their real test set is small (~29 images) and the 89.6% figure is after a post-processing adjustment, so this is a valid existence proof against a categorical \"will not,\" not proof that it transfers easily.\n\nREFUTED PART 2 — the \"because\" clause is the wrong mechanism, and this is the more important error. Three independent lines of evidence:\n\n(a) The same paper explicitly concludes the opposite: \"photorealism (which is expensive to achieve) is not the main quality driver of the trained network. Rather, most iteration steps consist only of small changes that optimize the data for the distinction between the different classes, rather than overall realism.\" What fixed their transfer was content — disease-pattern diversity, cluttered backgrounds, defocused branches, lighting variation — not lesion micro-texture fidelity.\n\n(b) Real lesion texture does not rescue transfer. In that same study, training on PlantVillage — genuine photographs of genuine infections, with all the real fine-scale appearance statistics the claim says are missing — produced results on their greenhouse photos \"barely better than random guessing.\" If real texture statistics were the operative variable, this could not happen.\n\n(c) Classifiers largely are not using lesion texture at all. Noyan (arXiv:2206.04374) trained on only 8 background pixels from PlantVillage images and got 49.0% accuracy against a 2.6% random-chance baseline. And real-to-real domain shift alone is devastating: Shibuya et al. (as cited in arXiv:2510.12909) report macro F1 of 98.2–99.5% same-field versus 49.6–87.6% cross-field, on all-real imagery, with domain-specific cues \"embedded in the background and within the leaf regions\" and background removal having \"only a limited impact.\"\n\nREFUTED PART 3 — Gazebo-specific premise. The claim treats rendered lesion appearance as something a shading model synthesizes and therefore degrades. In Gazebo it is not. SDF 1.11 material/pbr defines albedo_map, normal_map, roughness_map, metalness_map, ambient_occlusion_map, emissive_map, light_map and environment_map, each specified as \"Filename of the ... map\" — user-supplied image files. Texture a leaf with a macro photograph of real apple scab and the fine-scale statistics in the albedo map are real photographic statistics, merely resampled. Gazebo Harmonic renders these through Ogre 2.x with PBR support (per the official Gazebo docs). So the alleged deficiency is an asset-authoring choice, not an intrinsic limit of Gazebo rendering. The genuine Gazebo-specific limits are different ones: no subsurface scattering for leaf translucency, rasterization rather than path tracing, and texel density at the camera distances a SCOUT MINI would drive at.\n\nSCOPE CAVEATS AGAINST OVER-READING THE COUNTEREXAMPLE: Klein et al. is greenhouse tomato under controlled lighting. This project is open-field (노지) Korean apple in a tall-spindle orchard, where direct sun, hard shadow, wind motion and sky background make illumination variance far worse. The 89.6% does not transfer to that setting by assumption.\n\nPRACTICAL BEARING ON THE PROJECT: The right conclusion is not \"rendered texture is too coarse\" but \"the binding constraint is content diversity and domain shift, and real-image training does not escape it either.\" For fruit counting and pest/disease diagnosis, train on real apple imagery (field-collected plus public apple datasets) and use the Gazebo orchard for navigation, geometry, Nav2 tuning and MID-70 point-cloud simulation — not as the appearance source for the disease classifier. If synthetic imagery is used for perception at all, budget for iterative refinement and domain adaptation; both were required in every source examined.",
        "evidence": "PRIMARY SOURCES CONSULTED (all fetched directly):\n\n1. Klein J., Waller R., Pirk S., Palubicki W., Tester M., Michels D.L., \"Synthetic data at scale: a development model to efficiently leverage machine learning in agriculture,\" Frontiers in Plant Science, vol. 15, 16 Sept 2024. https://www.frontiersin.org/journals/plant-science/articles/10.3389/fpls.2024.1360113/full\n- \"A neural classifier is trained by exclusively using synthetic images, whose generation process is iteratively refined to obtain optimal performance.\"\n- Final accuracy 89.6% on real greenhouse tomato photographs; iteration 1 \"classified nearly all images as healthy.\"\n- PlantVillage (real images) training: \"classification results on our greenhouse photos are barely better than random guessing, indicating poor generalization.\"\n- \"photorealism (which is expensive to achieve) is not the main quality driver of the trained network.\"\n- Renderer: Blender + Adobe Substance 3D Designer for PBR maps (albedo, normal, AO, height).\n- Weakness noted: test set ~29 real images; 89.6% is post-adjustment.\n\n2. Noyan M.A., \"Uncovering bias in the PlantVillage dataset,\" arXiv:2206.04374. https://arxiv.org/abs/2206.04374\n- Model trained on \"only 8 pixels from the PlantVillage image backgrounds\" achieved \"49.0% accuracy on the held-out test set, well above the random guessing accuracy of 2.6%.\"\n\n3. \"Robust Plant Disease Diagnosis with Few Target-Domain Samples,\" arXiv:2510.12909. https://arxiv.org/html/2510.12909\n- Citing Shibuya et al.: same-field macro F1 \"98.2% to 99.5%\"; different-field \"49.6% to 87.6%\".\n- \"domain-specific characteristics are often embedded in the background and within the leaf regions\"; background removal \"had only a limited impact.\"\n- NOTE: these numbers are secondhand via this paper; I did not reach the Shibuya et al. original.\n\n4. SDFormat 1.11 material specification. https://github.com/gazebosim/sdformat/blob/main/sdf/1.11/material.sdf\n- PBR metal workflow: albedo_map, roughness_map, metalness_map, environment_map, ambient_occlusion_map, normal_map, emissive_map, light_map — each \"Filename of the ... map\", i.e. user-supplied texture image files.\n\n5. Gazebo Harmonic official docs, engine comparison. https://gazebosim.org/docs/harmonic/comparison/\n- Ogre 1.x supported, Ogre 2.x supported, OptiX \"partial support\"; \"Physically Based Rendering (PBR)\" available on engines that support it (Ogre 2), plus environment maps, lightmaps, particle effects.\n\n6. \"Bridging the Lab-to-Field gap in plant disease diagnosis through unsupervised domain adaptation enhanced by background recomposition,\" ScienceDirect S1574954125005886 — returned HTTP 403, could not verify directly. Search-snippet figures (FBR +3.5–23.6%, UDA +6.2–27.1%, UDA +41.3% for apple) are therefore UNVERIFIED and excluded from the correction's load-bearing reasoning.\n\nLIMITATION: WebSearch budget for this session was exhausted (200/200) partway through, so verification proceeded via direct WebFetch only. I found no study that trains specifically on Gazebo-rendered leaf imagery and tests on real orchard photographs; no primary source establishes or refutes the Gazebo-specific case directly. The refutation rests on (i) a purely-synthetic-rendered counterexample in a different engine, (ii) the mechanism being contradicted by its own authors and by background-bias/cross-field evidence, and (iii) the SDF spec showing lesion appearance is photo-texture-authored rather than shader-synthesized."
      },
      {
        "claim": "Apple orchard canopy significantly degrades GNSS accuracy and fix availability, so GNSS alone is not a reliable localization source between rows of a high-density apple orchard.",
        "verdict": "CONFIRMED",
        "correction": "The claim's substance holds and is the standard, repeatedly field-verified premise of orchard-robot navigation literature. Three precisions are needed, none of which overturn it:\n\n1. EVIDENCE IS ORCHARD-GENERIC, NOT APPLE-SPECIFIC. The direct field measurements I could reach are from apricot (5 m rows x 2.5 m trees, UC Davis), Y-trellis pear (6.0 x 3.0 m), Japanese netted orchards, and under-canopy row crops. I found NO primary source that measured GNSS fix rate or error inside a high-density tall-spindle APPLE orchard. The closest apple-specific work — Liu Weihong et al., \"Overall integrated navigation based on satellite and lidar in the standardized tall spindle apple orchards,\" Computers and Electronics in Agriculture 216:108489 (Jan 2024), DOI 10.1016/j.compag.2023.108489 — is paywalled (Unpaywall: is_oa=false; ScienceDirect/ResearchGate/MDPI all return 403). Its title corroborates the claim (state of the art fuses satellite AND lidar in exactly this orchard type) but I could not verify its numbers. Treat any specific figure attributed to it (e.g. a widely-repeated \"path tracking error < 0.06 m in dormant tall-spindle apple\") as UNVERIFIED — that snippet came from a search summary, not a source I could open, and note it is qualified \"dormant\" (leaf-off), i.e. the easy case.\n\n2. \"DEGRADED,\" NOT \"DENIED\" — AND IT IS SEASON- AND GEOMETRY-DEPENDENT. High-density tall-spindle apple is a narrow fruiting wall with an open alley, not a closed overhead canopy like corn under-canopy or a large-canopy traditional orchard. The dominant mechanism between rows is sky-view restriction (a high effective elevation mask in the cross-row direction, leaving an along-row sky strip) plus multipath off the tree wall, trellis posts and wires — so the typical failure mode is RTK dropping FIX->FLOAT and drifting to decimetre/metre error, not total signal loss. Fei & Vougioukas explicitly note the effect \"may not be as severe in the headlands,\" and their own apricot experiments spanned dormant, sparse-foliage and dense-foliage seasons. Note that your analytics window (fruit counting, leaf/fruit disease imagery) is leaf-on and fruit-loaded — the worst case, not the dormant case.\n\n3. THE ENGINEERING CONCLUSION IS \"NOT SOLE SOURCE,\" NOT \"USELESS.\" Every source I found reaches the same design answer: fuse GNSS with LiDAR/IMU/odometry and let LiDAR carry in-row localization. Do not read the claim as a reason to drop GNSS — it stays valuable for headland turns, row-to-row transitions, global georeferencing of collected imagery (which matters for your per-tree yield maps and disease hotspot maps), and as an EKF input with covariance inflated on FLOAT/DGPS status. For your specific project this is largely a simulation-fidelity question: if you model GNSS in Gazebo Harmonic, model it as intermittently degrading (fix->float, metre-level bias) between rows rather than as clean or absent, and make Nav2's localization depend on the MID-70 point cloud, not on GNSS.",
        "evidence": "PRIMARY SOURCE 1 (strongest — direct field statement, verbatim from the PDF text I extracted locally to /tmp/claude-1000/-home-myhome-YBNML/691d883b-bd7f-499c-9b36-a59b0bd14a8a/scratchpad/rowsense.txt):\nFei, Z. & Vougioukas, S. (UC Davis, Dept. of Biological and Agricultural Engineering), \"Row-sensing Templates: A Generic 3D Sensor-based Approach to Robot Localization with Respect to Orchard Row Centerlines,\" arXiv:2107.01321 (2021; published in Journal of Field Robotics).\n- \"Unlike localization in open fields where cm-level accurate GNSS ... signals are available, accurate and robust localization inside orchard rows cannot rely solely - or at all - on GNSS. The reason is that the foliage of tall trees ... often blocks GNSS signals or introduces multipath effects, rendering satellite-based localization with a moving GNSS receiver impossible or unreliable. This effect may not be as severe in the headlands between orchard blocks, although it can be present.\"\n- \"Inside the tree rows, the RTK-GNSS did not provide reliable localization because of tall canopies and foliage.\" (Section 4.3; apricot orchard, Davis CA, 2018-2019; average row spacing 5 m, trees every 2.5 m, rows ~50 m; tested dormant/winter, dense foliage/summer, sparse foliage/spring.)\n- Consequence in practice: \"Ground truth in the orchard was measured using a different approach ... because GNSS signals or RTK corrections were not available due to the foliage of large trees.\" They had to stretch a physical colored rope down the row centerline to obtain ground truth (rope method validated against RTK in the open at 0 +/- 0.012 m SD lateral, 0 +/- 0.00043 rad SD heading).\n- In their autonomous run, \"turning at the end of the row was initiated and guided by the RTK-GNSS, whereas the in-row navigation relied solely on the template-based localization\" — i.e. GNSS retained for headlands, replaced in-row.\n\nPRIMARY SOURCE 2: Xia et al., Frontiers in Plant Science, 2023 (\"Research on orchard navigation method based on fusion of 3D SLAM and point cloud positioning,\" PMC10330707). Y-trellis pear orchard, 6.0 m row x 3.0 m column spacing.\n- \"Due to the interference of the trellis and dense canopy, the GNSS signal is unstable, and the positioning method relying on GNSS alone is not reliable.\"\n- \"Agricultural robots often work under the plant canopy. This can result in satellite signals being blocked and not reaching GNSS receivers.\"\n- Their LiDAR NDT-SLAM fused with RTK achieved ~5 cm per-rank positioning, heading within 1 deg.\n\nPRIMARY SOURCE 3: Jiang, A. & Ahamed, T., Sensors (Basel), 2023, \"Navigation of an Autonomous Spraying Robot for Orchard Operations Using LiDAR for Tree Trunk Detection\" (PMC10223392). Japanese orchards, trees 3-5 m apart.\n- \"orchard navigation is the most complex, and interruptions occur in RTK-GNSS signals because high and dense canopies are frequently encountered.\"\n- They chose LiDAR as the single navigation sensor explicitly to \"overcome the limitations of low light and the interruption of RTK-GNSS signals.\"\n\nPRIMARY SOURCE 4 (quantitative, via OpenAlex abstract): \"GNSS and LiDAR Integrated Navigation Method in Orchards with Intermittent GNSS Dropout,\" Applied Sciences 14(8):3231, 2024, DOI 10.3390/app14083231.\n- \"Considering the vulnerability of satellite positioning signals to obstruction and interference in orchard environments...\" System switches to LiDAR positioning \"when the GNSS signal is unavailable.\"\n- \"The positioning experiment under satellite signal obstruction shows that compared to the GNSS/INS integrated with adaptive Kalman filtering, the navigation system proposed in this article reduced the average positioning error by 1.6 m.\" (i.e. GNSS/INS under orchard obstruction is metre-level, not centimetre-level.)\n\nPRIMARY SOURCE 5 (mechanism, adjacent crop): Sivakumar et al., \"Multi-Sensor Fusion based Robust Row Following for Compact Agricultural Robots,\" Field Robotics 2022, DOI 10.55417/fr.2022043. \"global navigation satellite system (GNSS) and other positioning sensors are prone to loss of accuracy due to attenuation and multi-path errors caused by crop leaves and stems.\" Also Gasparino et al., RSS 2021, \"Learned Visual Navigation for Under-Canopy Agricultural Robots\" (roboticsproceedings.org/rss17/p019.pdf): robots \"cannot rely on RTK ... GPS based methods which are used for over-the-canopy autonomy ... because of GPS signal attenuation and multipath errors.\"\n\nCORROBORATING BUT UNREAD (apple tall-spindle specific): Liu Weihong, Li Wenwei, Feng Han, Xu Jiawei, Yang Shenghui, Zheng Yongjun, Liu Xingxing, Wang Zimeng, Yi Xiaokang, He Yichuan, Zhou Ling — \"Overall integrated navigation based on satellite and lidar in the standardized tall spindle apple orchards,\" Computers and Electronics in Agriculture 216:108489, Jan 2024 (Crossref-verified metadata; Unpaywall is_oa=false; paywalled, abstract not retrievable via ScienceDirect/ResearchGate/MDPI/CoLab/Semantic Scholar/OpenAlex).\n\nMETHOD NOTE: WebSearch budget for the session was exhausted mid-task (200/200); remaining retrieval was done via direct WebFetch, curl, and the OpenAlex/Crossref/Semantic Scholar/Unpaywall APIs. Helper script at /tmp/claude-1000/-home-myhome-YBNML/691d883b-bd7f-499c-9b36-a59b0bd14a8a/scratchpad/oa.py; extracted paper text at /tmp/claude-1000/-home-myhome-YBNML/691d883b-bd7f-499c-9b36-a59b0bd14a8a/scratchpad/rowsense.txt."
      },
      {
        "claim": "FAST-LIO2 / Point-LIO are LiDAR-inertial odometry packages that Livox sensors are commonly used with, and they can operate with the narrow FOV of a Livox solid-state LiDAR.",
        "verdict": "CONFIRMED",
        "correction": "The claim as literally stated is accurate and backed by primary sources. No correction to the claim itself is needed. However, it is stated at a level of generality that hides four caveats that are decisive for THIS project (MID-70 + ROS 2 Jazzy + orchard rows):\n\n1. \"Can operate with narrow FOV\" is not \"narrow FOV is a solved problem.\" The FAST-LIO authors state the opposite risk explicitly in the RA-L 2021 paper (arXiv:2010.08196, Sec. I): \"When the UAV is operating in cluttered environments where no strong features are present, the LiDAR-based solution easily degenerates. This problem is more obvious when the LiDAR has a small FoV.\" And in Sec. II-A: prior LOAM variants \"work well for structured environments and LiDARs of large FoV, they are very vulnerable to featureless environments or small FoV LiDARs.\" Tight IMU coupling mitigates this; it does not remove it. A forward-facing 70.4-degree MID-70 driving down a long, geometrically repetitive tall-spindle apple row is close to the textbook corridor/tunnel degeneracy case (weak along-row translational observability). Expect to need wheel odometry from the SCOUT MINI fused in, or a non-forward (e.g. tilted/side-canted) LiDAR mount, or both. Neither FAST-LIO2 nor Point-LIO ingests wheel odometry natively.\n\n2. Point-LIO has NO official ROS 2 port. github.com/hku-mars/Point-LIO has only two branches (\"master\", last updated 2024-05-20, and \"point-lio-with-grid-map\"). Its README says \"We tested our code on Ubuntu20.04 with noetic.\" Using it on ROS 2 Jazzy requires a third-party fork. FAST_LIO does have an official ROS2 branch, but its README states \"ROS >= Foxy (Recommend to use ROS-Humble)\" — Jazzy is not listed or claimed as tested.\n\n3. Driver/message-path mismatch for MID-70 specifically. Both packages require the Livox CustomMsg per-point-timestamp path, not plain PointCloud2: \"For livox serials, FAST-LIO only support the data collected by the livox_lidar_msg.launch since only its livox_ros_driver/CustomMsg data structure produces the timestamp of each LiDAR point.\" Point-LIO's README says the same. But the FAST_LIO ROS2 branch depends on livox_ros_driver2, whose README lists under \"Supported LiDAR list\" only HAP and Mid360 — MID-70 and Avia are not supported by driver2 at all. MID-70 is served by the legacy livox_ros_driver (ROS 1) / livox_ros2_driver (Foxy-era). In simulation this means you must synthesize livox_ros_driver/CustomMsg yourself from the Gazebo sensor; a stock gz-sim gpu_lidar PointCloud2 will not drive either package's Livox path.\n\n4. MID-70 support is hedged in the source, and MID-70 has no built-in IMU. FAST_LIO's top-level feature list does name it (\"solid-state (Livox Avia, Horizon, MID-70) LiDARs\"), but the run instructions say only \"mapping_avia.launch theratically [sic] supports mid-70, mid-40 or other livox serial LiDAR, but need to setup some parameters befor run.\" Point-LIO uses the identical hedge. All the papers' and READMEs' actual small-FOV experiments are on Livox AVIA (70 deg FOV), which has an integrated IMU. The Livox Mid-70 spec sheet has no IMU row — so a LIO package that mandates IMU input requires you to add an external IMU with your own extrinsic calibration and hardware time sync. That is extra work the claim glosses over.",
        "evidence": "PART A — \"Livox sensors are commonly used with them\": CONFIRMED.\n- github.com/hku-mars/FAST_LIO README (main), line 44: \"Since no requirements for feature extraction, FAST-LIO2 can support many types of LiDAR including spinning (Velodyne, Ouster) and solid-state (Livox Avia, Horizon, MID-70) LiDARs, and can be easily extended to support more LiDARs.\" Line 80: \"Since the FAST-LIO must support Livox serials LiDAR firstly, so the livox_ros_driver must be installed and sourced before run any FAST-LIO luanch file.\" Livox is a hard build dependency; the flagship launch file is mapping_avia.launch.\n- github.com/hku-mars/Point-LIO README: same Livox-first structure — livox_ros_driver required, mapping_avia.launch is the primary Livox entry point, \"Point-LIO only support the data collected by the livox_lidar_msg.launch.\"\n- Both packages are from the HKU MARS Lab (Fu Zhang's group), the group historically tied to Livox hardware (LOAM-Livox, livox_camera_calib, FAST-LIVO all from the same lab).\n\nPART B — \"can operate with the narrow FOV of a Livox solid-state LiDAR\": CONFIRMED, explicitly.\n- FAST-LIO2 paper, arXiv:2107.06829 (submitted 2021-07-14), published as W. Xu, Y. Cai, D. He, J. Lin, F. Zhang, \"FAST-LIO2: Fast Direct LiDAR-Inertial Odometry,\" IEEE Transactions on Robotics, vol. 38, no. 4, pp. 2053-2073, Aug. 2022, DOI 10.1109/TRO.2022.3141876 (verified via Crossref). Abstract: \"Various real-world experiments on solid-state LiDARs with small FoV are also conducted.\" Also states it is \"applicable to both multi-line spinning and solid-state LiDARs.\"\n- FAST-LIO v1 paper, W. Xu and F. Zhang, arXiv:2010.08196 (submitted 2020-10-16), RA-L 2021. Sec. V-B (text extracted from the PDF): \"we build a small-scale quadrotor that can carry a Livox Avia LiDAR with 70 deg FoV and a DJI Manifold 2-C onboard computer... the drift is smaller than 0.3% (0.08 m drift over 32 m trajectory).\" So a 70-degree-FOV solid-state Livox is the reference platform, not an edge case.\n- FAST_LIO README (both main and ROS2 branches) repeats: \"we build a small-scale quadrotor which can carry a Livox Avia LiDAR with 70 degree FoV.\"\n- Point-LIO: D. He, W. Xu, N. Chen, F. Kong, C. Yuan, F. Zhang, \"Point-LIO: Robust High-Bandwidth Light Detection and Ranging Inertial Odometry,\" Advanced Intelligent Systems, vol. 5, no. 7, art. 2200459; published online 2023-04-07, print July 2023, DOI 10.1002/aisy.202200459 (verified via Crossref; Wiley full text returned HTTP 402 so the abstract could not be quoted directly). Its README's primary demo LiDAR is the Livox Avia (70.4 deg circular FOV).\n\nSUPPORTING FACTS FOR THE CAVEATS:\n- Livox official Mid-70 pages (livoxtech.com/mid-70 and /mid-70/specs): FOV \"70.4 deg (Circular)\", horizontally and vertically; 100,000 points/s (first/strongest return), 580 g, 97x64x62.7 mm. No IMU row in the specification table. (This corroborates the FOV figure given in the task brief.)\n- github.com/Livox-SDK/livox_ros_driver2 README, section \"5. Supported LiDAR list\": only HAP and Mid360. Mid-70 and Avia appear nowhere. Its OS matrix does list ROS2 Jazzy on Ubuntu 24.04, but \"foxy or humble recommended.\"\n- github.com/Livox-SDK/livox_ros_driver (v1) repo description: \"Livox device driver under ros, support Lidar Mid-40, Mid-70, Tele-15, Horizon, Avia\" — i.e., MID-70 lives on the ROS 1 / SDK1 driver.\n- github.com/hku-mars/FAST_LIO ROS2 branch README, sec 1.1: \"Ubuntu >= 20.04\", \"ROS >= Foxy (Recommend to use ROS-Humble)\"; sec 1.3 requires livox_ros_driver2; its live-sensor example invokes \"ros2 launch livox_ros_driver2 msg_MID360_launch.py\".\n- github.com/hku-mars/Point-LIO/branches/all: branches are \"master\" (updated 2024-05-20) and \"point-lio-with-grid-map\" only — no ROS2 branch."
      },
      {
        "claim": "Modern high-density tall-spindle apple orchards typically use row spacing on the order of 3 to 4 metres and in-row tree spacing on the order of 0.8 to 1.5 metres.",
        "verdict": "CONFIRMED",
        "correction": "The claim survives scrutiny — every primary source I found falls inside or within centimetres of the stated envelope. Two precision refinements, neither of which overturns it:\n\n(1) ROW SPACING is well supported, but the true full range is ~2.8-3.9 m rather than exactly 3-4 m. Robinson's canonical Tall Spindle is 3.0-3.3 m on level ground and 3.6-3.9 m on slopes; Korean 세장방추형 extension guidance dips to 2.8 m at the low end. Nothing in the tall-spindle literature reaches a full 4.0 m — 4.0 m appears only for the wider 방추형 (standard spindle), not 세장방추형. For an orchard sim, 3.0-3.5 m is the realistic centre of mass, and 3.5 m matches RDA's most-cited Korean figure.\n\n(2) IN-ROW SPACING: the 0.8-1.5 m band is correct as an envelope but is wider than the strict Cornell definition. Robinson et al. specify only 0.9 m (3 ft) for weak/moderate cultivars and 1.2 m (4 ft) for vigorous ones — i.e. 0.9-1.2 m, never 1.5 m. The 1.5 m upper bound is justified by Korean practice, not US practice: RDA gives 1.2-1.5 m on medium-fertility soils and goes to 1.8 m on high-fertility soils. MSU Extension allows down to 0.6 m (2 ft), below the claim's 0.8 m floor, though it names 0.9 m (3 ft) as most common. So for the Korean apple orchard in this project, 1.2-1.5 m in-row is the defensible choice and 0.8 m would be unusually tight.\n\nTERMINOLOGY CAVEAT relevant to this project: Korean 세장방추형 translates literally as \"slender spindle\" (Wertheim's system as adapted locally); Robinson's \"Tall Spindle\" is normally rendered 키큰방추형 in Korean extension material. The two systems overlap heavily in geometry but are not strictly the same system, so citing Cornell Tall Spindle numbers as 세장방추형 numbers is a mild conflation — harmless at simulation fidelity, but worth knowing.\n\nSIMULATION IMPLICATION (not part of the claim, but load-bearing for the Nav2/MID-70 design): mature canopy width in these systems is 0.9-1.5 m (Robinson: tree diameter 0.9-1.2 m; 한국사과협회: 나무 폭 1.5m 내외). At 3.0-3.5 m row spacing that leaves a free alley of roughly 1.6-2.4 m between canopy faces, with tree height 3.0-3.5 m. A SCOUT MINI (~0.61 m wide) has ample clearance, but the alley is narrow enough that a 70.4-degree MID-70 FOV will be filled by canopy at close range.",
        "evidence": "PRIMARY SOURCE 1 — the defining paper by the system's originator. Robinson, T.L., Hoying, S.A., Reginato, G.H. (2006), \"The Tall Spindle Apple Production System,\" New York Fruit Quarterly 14(2):21. Cornell NYSAES. Verbatim: \"For weak and moderate growing cultivars such as Honeycrisp, Delicious, Braeburn, Empire, Jonamac, Macoun, Idared, Gala, NY674, Golden Delicious, etc., we suggest an in-row spacing of 3ft (90cm). For vigorous varieties such as McIntosh, Spartan, Fuji, Jonagold, Mutsu, etc., and tip bearing varieties such as Cortland, Rome, Granny Smith and Gingergold we suggest an in-row spacing of 4ft (1.2m) Between-row spacing should be 12-13 ft (3.6-3.9m) on slopes and 10-11ft (3.0-3.3 m) on level ground.\" Also: \"the Tall Spindle Tree with a tree diameter of 3-4 ft (90cm-1.2m), a tree height of 10 ft (3m) and a density between 1,000 and 1,500 trees/acre\" (= approx. 2,470-3,700 trees/ha). Cost table is headed \"Establishment Costs for 3' X 11' Tall Spindle Orchard System\". https://nyshs.org/wp-content/uploads/2016/10/The-Tall-Spindle-Apple-Production-System.pdf\n\nPRIMARY SOURCE 2 — Hoying, S., Robinson, T., Fargione, M., \"The Tall Spindle Planting System,\" Cornell Cooperative Extension, Hudson Valley Lab and NYSAES, Tree Fruit Resources factsheet. Verbatim: \"High density - 1000 - 1500 trees per acre... The maximum spacing between trees should [be] 4 feet and 12 feet between rows. The optimum Tall Spindle spacing for an average vigor variety and soil is 3 feet by 11 feet\" (= 0.91 m x 3.35 m). https://bpb-us-e1.wpmucdn.com/blogs.cornell.edu/dist/0/7265/files/2016/12/The-Tall-Spindle-Planting-System-2746zv0.pdf\n\nPRIMARY SOURCE 3 — Michigan State University Extension (R. Perry), \"HD and Tall Spindle\" training presentation. \"Tall Spindle System ... Spacing: * Tree Row: 2-4 ft apart (3 ft most common) * Alleyway space: 10-11 ft\" = in-row 0.61-1.22 m (0.91 m most common), rows 3.05-3.35 m. Also gives the design rule \"Limit height of tree to 80-90% of between row spacing.\" https://www.canr.msu.edu/hrt/uploads/535/78649/HD-and-Tall-Spindle-12.pdf\n\nPRIMARY SOURCE 4 (Korea, national) — RDA 농사로 (Nongsaro), Rural Development Administration apple cultivation schedule. Planting distances for Hongro/Fuji on spindle training: medium-fertility soil 3.2 x 1.2 m (260 trees/10a) and 3.5 x 1.5 m (190 trees/10a); high-fertility soil 3.8 x 1.5 m (175 trees/10a) and 3.8 x 1.8 m (146 trees/10a). https://www.nongsaro.go.kr/portal/ps/psb/psbl/workScheduleDtl.ps?menuId=PS00087&cntntsNo=30663\n\nPRIMARY SOURCE 5 (Korea, provincial) — 충청북도 농업기술원 (Chungbuk ARES), apple training-system table. 세장방추형: \"2.8~3.5m x 1.0~1.5m (357~190주/10a)\", rootstock M.9, trellis/support mandatory (지주설치: 필수). By contrast plain 방추형: 3.0-4.0 m x 1.2-2.0 m. https://ares.chungbuk.go.kr/home/sub.php?menukey=1161\n\nCROSS-CHECK ON ARITHMETIC: 3.5 x 1.5 m = 5.25 m^2/tree; 1,000 m^2 (10a) / 5.25 = 190 trees — matches the cited 190주/10a exactly, so the Korean figures are internally consistent. Cornell 3 ft x 11 ft = 33 ft^2; 43,560/33 = 1,320 trees/acre — matches the \"Trees 1320\" line in the establishment-cost table exactly.\n\nCONVERGENCE: four independent authorities on two continents (Cornell/NYSAES, MSU Extension, RDA Nongsaro, Chungbuk ARES) all land inside or within 0.2 m of the claimed 3-4 m row / 0.8-1.5 m in-row envelope. The claim is correctly hedged (\"typically\", \"on the order of\") and is not overstated."
      },
      {
        "claim": "Nav2 on ROS 2 Jazzy provides the MPPI controller and the Smac Planner family, and these are better suited to a skid-steer robot navigating narrow orchard rows than the legacy DWB controller.",
        "verdict": "PARTIALLY_TRUE",
        "correction": "Three separable assertions; only the first survives intact.\n\n1) AVAILABILITY — CONFIRMED. Both `nav2_mppi_controller` and `nav2_smac_planner` are released into Jazzy at version 1.3.12 (Humble 1.1.20, Kilted 1.4.2). `nav2_smac_planner` ships three plugins: SmacPlanner2D, SmacPlannerHybrid (Hybrid-A*), SmacPlannerLattice (State Lattice).\n\n2) \"LEGACY DWB\" — REFUTED, twice over.\n   (a) DWB is not deprecated or legacy. `dwb_core` is released in Jazzy at 1.3.12, is still documented in the Nav2 configuration guide and the plugin index, and no Nav2 document uses the words \"legacy\" or \"deprecated\" for it. Nav2's tuning guide says MPPI is \"a far more modern and advanced technique\" and \"highly recommended\" — a preference, not a deprecation.\n   (b) The claim's implicit premise that DWB is the thing you get by default on Jazzy is also wrong. `nav2_bringup/params/nav2_params.yaml` has shipped `nav2_mppi_controller::MPPIController` as the `FollowPath` plugin since the very first Jazzy tag (1.3.0) and still does at 1.3.12. MPPI is the Jazzy default; you have to opt *in* to DWB. (Nav2's own `select_algorithm` page still says \"The default controller plugin is the DWB controller\" — that sentence is stale documentation, contradicted by the shipped YAML.)\n\n3) \"BETTER SUITED TO SKID-STEER IN NARROW ORCHARD ROWS\" — OVERSTATED, and the Smac half is substantively wrong.\n   (a) Category error: Smac planners are *global planners* (planner_server); DWB is a *local controller* (controller_server). They are not alternatives. The apples-to-apples pairs are MPPI vs DWB, and Smac vs NavFn/Theta*.\n   (b) \"The Smac Planner family\" is the wrong recommendation for a SCOUT MINI. Per Nav2's own selection table:\n       - SmacPlannerHybrid → \"Non-circular or Circular Ackermann, Non-circular or Circular Legged\". Not differential. It plans on Dubin/Reeds-Shepp with a `minimum_turning_radius`, which discards the skid-steer's ability to pivot in place — actively harmful at orchard row-end headland turns where in-place rotation is the cheapest maneuver.\n       - SmacPlanner2D → grouped with NavFn and Theta* under \"Circular Differential, Circular Omnidirectional\" only, and does no SE2 footprint collision checking. The SCOUT MINI is a rectangular ~0.61 x 0.58 m base, i.e. exactly the case where footprint orientation matters in a narrow row.\n       - Only SmacPlannerLattice is listed for \"Non-circular Differential\". That is the one defensible Smac choice here — but its shipped defaults are Ackermann: \"The defaults for State Lattice were generated using the 5cm Ackermann files.\" You must generate a differential minimum control set (including rotate-in-place primitives, gated by `rotation_penalty`) or it will plan Ackermann-style arcs for a robot that can spin on the spot.\n   (c) MPPI's edge over DWB is not skid-steer support — DWB explicitly supports Differential and Omnidirectional too. The real edge is optimization-based predictive trajectories vs DWB's constant-action model, plus Ackermann/Legged coverage. Both are listed for the same task (\"Dynamic obstacle avoidance\").\n   (d) Narrow spaces are the one place MPPI is *not* free. Its own README warns that if the Obstacle critic's `repulsion_weight` is too high relative to `inflation_radius`, \"the robot will slow significantly when entering cost-space from non-cost space or jitter in narrow corridors.\" Narrow-corridor performance is a tuning outcome, not an out-of-the-box property.\n   (e) No primary source found validates either MPPI or Smac specifically in orchard rows. That sub-claim is UNVERIFIABLE.\n\nPROJECT-SPECIFIC CAVEAT (not part of the claim, but it undercuts the practical benefit): both MPPI's Obstacle/Cost critics and Smac's collision checking read the costmap, and the costmap is only as good as sensor coverage. A Livox MID-70 has a ~70.4-degree circular FOV with no lateral or rear coverage, so marked-but-unclearable cells and blind flanks will dominate behavior in a narrow row far more than the MPPI-vs-DWB choice does. Compute is a non-issue: MPPI is measured at 50+ Hz on a 4th-gen i5 and is CPU-only, so the i5-13500 is ample and the broken GTX 1660 SUPER driver does not affect it.\n\nDEFENSIBLE RESTATEMENT: \"Nav2 Jazzy ships MPPI (the Jazzy default controller) and the Smac Planner family. MPPI is the maintainers' recommended controller over DWB and is a reasonable choice for the SCOUT MINI, though narrow-row behavior depends on tuning repulsion_weight against inflation_radius. For the global planner, the SCOUT MINI's rectangular footprint argues for SmacPlannerLattice with a custom-generated differential control set — not Hybrid-A*, which is for Ackermann/legged, and not Smac 2D, which assumes a circular robot.\"",
        "evidence": "PRIMARY SOURCES (all fetched 2026-07-24):\n\nRelease/version facts:\n- https://index.ros.org/p/nav2_mppi_controller/ — Jazzy 1.3.12, Humble 1.1.20, Kilted 1.4.2. Status RECOMMENDED, MIT.\n- https://index.ros.org/p/nav2_smac_planner/ — Jazzy 1.3.12, Humble 1.1.20, Kilted 1.4.2.\n- https://raw.githubusercontent.com/ros-navigation/navigation2/jazzy/nav2_dwb_controller/dwb_core/package.xml — `<version>1.3.12</version>`, no deprecation notice. DWB is a live, released Jazzy package.\n- Jazzy branch identity verified: nav2_bringup package.xml on branch `jazzy` = 1.3.12, matching the released Jazzy version (main = 1.4.0).\n\nDefault-controller fact (contradicts \"legacy DWB is what you get\"):\n- Checked release tags 1.3.0, 1.3.5 and 1.3.12 of nav2_bringup/params/nav2_params.yaml. All three: `controller_plugins: [\"FollowPath\"]` / `FollowPath: plugin: \"nav2_mppi_controller::MPPIController\"`, and `plugin: \"nav2_navfn_planner::NavfnPlanner\"` for the planner. MPPI has been the Jazzy default from 1.3.0 onward; NavFn (not Smac) is the default planner.\n- Stale doc: https://raw.githubusercontent.com/ros-navigation/docs.nav2.org/master/setup_guides/algorithm/select_algorithm.rst line 76 — \"The default controller plugin is the `DWB controller`\". Contradicted by the shipped YAML above.\n\nNav2 maintainer selection tables — https://raw.githubusercontent.com/ros-navigation/docs.nav2.org/master/tuning/index.rst (saved to /tmp/claude-1000/-home-myhome-YBNML/691d883b-bd7f-499c-9b36-a59b0bd14a8a/scratchpad/tuning.rst):\n- Planner table (lines 58-70):\n    NavFn / Smac Planner 2D / Theta Star  → \"Circular Differential, Circular Omnidirectional\"\n    Smac Hybrid-A* Planner                → \"Non-circular or Circular Ackermann, Non-circular or Circular Legged\"\n    Smac Lattice Planner                  → \"Non-circular Differential, Non-circular Omnidirectional, Arbitrary\"\n  Line 74: \"Since the planning problem is primarily driven by the robot type, the table accurately summarizes the advice to users by the maintainers.\"\n  Line 34: \"If you plan to use a holonomic planner (e.g. Theta*, Smac 2D-A*, or NavFn), you may continue to use the circular footprint, since these planners are not kinematically feasible and will not make use of the SE2 footprint anyway.\"\n- Controller table (lines 86-98):\n    DWB controller → \"Differential, Omnidirectional\" / \"Dynamic obstacle avoidance\"\n    MPPI Controller → \"Differential, Omnidirectional, Ackermann, Legged\" / \"Dynamic obstacle avoidance\"\n  Line 100: \"All of the above controllers can handle both circular and arbitrary shaped robots in configuration.\"\n  Line 106: \"MPPI is a far more modern and advanced technique ... MPPI however does have moderately higher compute costs, but it is highly recommended to go this route.\" No use of \"legacy\" or \"deprecated\" for DWB anywhere in the file.\n\nMPPI narrow-corridor caveat and compute — https://raw.githubusercontent.com/ros-navigation/navigation2/jazzy/nav2_mppi_controller/README.md (saved to .../scratchpad/mppi_jazzy.md):\n- Line 11: \"This controller is measured to run at 50+ Hz on a modest Intel processor (4th gen i5).\"\n- Line 13: \"It works currently with Differential, Omnidirectional, and Ackermann robots.\" (no separate skid-steer model; `motion_model` enum is [DiffDrive, Omni, Ackermann], line 39)\n- Line 300: \"If this penalty is too high, the robot will slow significantly when entering cost-space from non-cost space or jitter in narrow corridors.\"\n\nSmac skid-steer caveat — https://raw.githubusercontent.com/ros-navigation/navigation2/jazzy/nav2_smac_planner/README.md (saved to .../scratchpad/smac_jazzy.md):\n- Line 24: \"We support **circular** differential-drive and omni-directional drive robots using the `SmacPlanner2D` ... We support **legged, cars, car-like, and ackermann vehicles** using the `SmacPlannerHybrid` ... We support **non-circular, arbitrary shaped, any model vehicles** using the `SmacPlannerLattice`.\"\n- Line 186: \"The defaults for State Lattice were generated using the 5cm Ackermann files you can find in this package as initial examples.\"\n- Line 127: `rotation_penalty` — \"Penalty to apply only to pure rotate in place commands when using minimum control sets containing rotate in place primitives\" (i.e. in-place rotation exists only if the control set provides it).\n- Line 168: min turning radius \"for differential or omni robots, its a bit of a dance around what kind of turns you'd like your robot to be able to make.\"\n\nNOT FOUND / UNVERIFIABLE: no primary source (Nav2 docs, package READMEs, or peer-reviewed/extension publication) evaluating MPPI or Smac planners in orchard rows or any narrow agricultural row environment. The orchard-specific suitability portion of the claim rests on no evidence I could locate.\n\nLIMITATION: WebSearch budget (200 calls) was exhausted for this session before I could search for a formal DWB deprecation announcement on ROS Discourse or the Nav2 migration guide. My \"DWB is not deprecated\" finding rests on the package being actively released at 1.3.12 in Jazzy and on the absence of any deprecation language in the Jazzy-branch package.xml and current docs — strong but not exhaustive."
      }
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Research"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Verify"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "research:nav2",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "ac16fb4077b79404a",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784901948362,
      "queuedAt": 1784901945179,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Nav2's default stack does not fail catastrophically with a …",
      "promptPreview": "TARGET MACHINE (verified, ground truth): Ubuntu 24.04.4 noble, i5-13500 (20 threads), 31 GiB RAM,\nGTX 1660 SUPER (TU116, no RT cores) with a currently-BROKEN driver (Secure Boot blocks the DKMS module),\nIntel Alder Lake iGPU as fallback, Wayland session, no ROS/Gazebo/Docker installed yet.\nPlanned stack: ROS 2 Jazzy + Gazebo Harmonic (gz-sim 8).\n\nPROJECT: An AgileX SCOUT MINI (4WD skid-steer) carr…",
      "lastProgressAt": 1784902541393,
      "tokens": 65075,
      "toolCalls": 37,
      "durationMs": 593031,
      "resultPreview": "{\"summary\":\"Nav2's default stack does not fail catastrophically with a Livox MID-70, but three specific things break: (1) AMCL loses along-row observability because a 70.4° forward cone in a self-similar tree row provides almost no longitudinal constraint — this is the same degeneracy FAST-LIO papers document for narrow-FOV Livox units in corridors; (2) the costmap obstacle layer only marks and on…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "research:apple",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "a297a37bc14fa9697",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784901948019,
      "queuedAt": 1784901945179,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Research complete on all 5 dimensions. Headline findings: (…",
      "promptPreview": "TARGET MACHINE (verified, ground truth): Ubuntu 24.04.4 noble, i5-13500 (20 threads), 31 GiB RAM,\nGTX 1660 SUPER (TU116, no RT cores) with a currently-BROKEN driver (Secure Boot blocks the DKMS module),\nIntel Alder Lake iGPU as fallback, Wayland session, no ROS/Gazebo/Docker installed yet.\nPlanned stack: ROS 2 Jazzy + Gazebo Harmonic (gz-sim 8).\n\nPROJECT: An AgileX SCOUT MINI (4WD skid-steer) carr…",
      "lastProgressAt": 1784902605501,
      "tokens": 76256,
      "toolCalls": 40,
      "durationMs": 657482,
      "resultPreview": "{\"summary\":\"Research complete on all 5 dimensions. Headline findings:\\n\\n(1) GEOMETRY — Korean practice differs meaningfully from US/EU. RDA/농사로 official planting distances are 3.2×1.2 m (260주/10a) to 3.8×1.8 m (146주/10a); the standard 세장방추형 is ~3.5×1.5 m, 190주/10a (=1,900 trees/ha), tree height 3–3.5 m. RDA is now pushing 키큰방추형 (tall spindle proper) at ~1 m in-row, >300주/10a, height 4–4.5 m. US C…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "research:disease",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "a42d70f6c9f1870db",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784901949202,
      "queuedAt": 1784901945180,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Blunt verdict up front: a disease classifier trained on Gaz…",
      "promptPreview": "TARGET MACHINE (verified, ground truth): Ubuntu 24.04.4 noble, i5-13500 (20 threads), 31 GiB RAM,\nGTX 1660 SUPER (TU116, no RT cores) with a currently-BROKEN driver (Secure Boot blocks the DKMS module),\nIntel Alder Lake iGPU as fallback, Wayland session, no ROS/Gazebo/Docker installed yet.\nPlanned stack: ROS 2 Jazzy + Gazebo Harmonic (gz-sim 8).\n\nPROJECT: An AgileX SCOUT MINI (4WD skid-steer) carr…",
      "lastProgressAt": 1784902763523,
      "tokens": 88738,
      "toolCalls": 56,
      "durationMs": 814321,
      "resultPreview": "{\"summary\":\"Blunt verdict up front: a disease classifier trained on Gazebo-rendered apple imagery will NOT transfer to a real Korean orchard at useful accuracy. The literature is consistent — synthetic-only fruit detection loses ~33 mAP points vs real-only (0.656 vs 0.983), and procedural synthetic-only plant-disease classification lands around F1 0.40 on a 4-class problem. Meanwhile the diagnosti…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "verify:1",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "abe93320f87102f61",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784902769471,
      "queuedAt": 1784902767395,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "A Livox MID-70 has a circular field of view of about 70.4 d…",
      "promptPreview": "TARGET MACHINE (verified, ground truth): Ubuntu 24.04.4 noble, i5-13500 (20 threads), 31 GiB RAM,\nGTX 1660 SUPER (TU116, no RT cores) with a currently-BROKEN driver (Secure Boot blocks the DKMS module),\nIntel Alder Lake iGPU as fallback, Wayland session, no ROS/Gazebo/Docker installed yet.\nPlanned stack: ROS 2 Jazzy + Gazebo Harmonic (gz-sim 8).\n\nPROJECT: An AgileX SCOUT MINI (4WD skid-steer) carr…",
      "lastProgressAt": 1784902997850,
      "tokens": 35302,
      "toolCalls": 18,
      "durationMs": 228378,
      "resultPreview": "{\"claim\":\"A Livox MID-70 has a circular field of view of about 70.4 degrees and is therefore unsuitable on its own as the sole sensor for Nav2 AMCL localization and a 360-degree obstacle costmap; a practical orchard Nav2 setup needs either an additional wider-FOV sensor, a persistent/voxel costmap layer, or GNSS+IMU+odometry fusion.\",\"verdict\":\"PARTIALLY_TRUE\",\"correction\":\"The hardware number is …"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "verify:2",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ab6ea45dd9aca4608",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784902769590,
      "queuedAt": 1784902767395,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Gazebo Harmonic (gz-sim 8) ships a segmentation camera sens…",
      "promptPreview": "TARGET MACHINE (verified, ground truth): Ubuntu 24.04.4 noble, i5-13500 (20 threads), 31 GiB RAM,\nGTX 1660 SUPER (TU116, no RT cores) with a currently-BROKEN driver (Secure Boot blocks the DKMS module),\nIntel Alder Lake iGPU as fallback, Wayland session, no ROS/Gazebo/Docker installed yet.\nPlanned stack: ROS 2 Jazzy + Gazebo Harmonic (gz-sim 8).\n\nPROJECT: An AgileX SCOUT MINI (4WD skid-steer) carr…",
      "lastProgressAt": 1784903013754,
      "tokens": 34856,
      "toolCalls": 21,
      "durationMs": 244163,
      "resultPreview": "{\"claim\":\"Gazebo Harmonic (gz-sim 8) ships a segmentation camera sensor supporting both semantic and panoptic/instance modes, and a bounding-box camera sensor supporting 2D and 3D boxes, both bridgeable to ROS 2.\",\"verdict\":\"CONFIRMED\",\"correction\":\"The claim is accurate as stated, but four refinements matter before designing around it:\\n\\n1. \\\"panoptic/instance\\\" is ONE mode, not two. `<segmentat…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "verify:3",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "af6df3763296a2dd1",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784902770236,
      "queuedAt": 1784902767396,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "A plant-disease classifier trained purely on Gazebo-rendere…",
      "promptPreview": "TARGET MACHINE (verified, ground truth): Ubuntu 24.04.4 noble, i5-13500 (20 threads), 31 GiB RAM,\nGTX 1660 SUPER (TU116, no RT cores) with a currently-BROKEN driver (Secure Boot blocks the DKMS module),\nIntel Alder Lake iGPU as fallback, Wayland session, no ROS/Gazebo/Docker installed yet.\nPlanned stack: ROS 2 Jazzy + Gazebo Harmonic (gz-sim 8).\n\nPROJECT: An AgileX SCOUT MINI (4WD skid-steer) carr…",
      "lastProgressAt": 1784902965744,
      "tokens": 32089,
      "toolCalls": 17,
      "durationMs": 195508,
      "resultPreview": "{\"claim\":\"A plant-disease classifier trained purely on Gazebo-rendered synthetic leaf imagery will not transfer reliably to real orchard photographs, because rendered lesion texture lacks the fine-scale appearance statistics of real infections.\",\"verdict\":\"PARTIALLY_TRUE\",\"correction\":\"The warning is directionally sound but the claim is overstated in its conclusion and its stated causal mechanism …"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "verify:4",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ab798ac15b6567aa6",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784902771044,
      "queuedAt": 1784902767396,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Apple orchard canopy significantly degrades GNSS accuracy a…",
      "promptPreview": "TARGET MACHINE (verified, ground truth): Ubuntu 24.04.4 noble, i5-13500 (20 threads), 31 GiB RAM,\nGTX 1660 SUPER (TU116, no RT cores) with a currently-BROKEN driver (Secure Boot blocks the DKMS module),\nIntel Alder Lake iGPU as fallback, Wayland session, no ROS/Gazebo/Docker installed yet.\nPlanned stack: ROS 2 Jazzy + Gazebo Harmonic (gz-sim 8).\n\nPROJECT: An AgileX SCOUT MINI (4WD skid-steer) carr…",
      "lastProgressAt": 1784903209192,
      "tokens": 61671,
      "toolCalls": 33,
      "durationMs": 438147,
      "resultPreview": "{\"claim\":\"Apple orchard canopy significantly degrades GNSS accuracy and fix availability, so GNSS alone is not a reliable localization source between rows of a high-density apple orchard.\",\"verdict\":\"CONFIRMED\",\"correction\":\"The claim's substance holds and is the standard, repeatedly field-verified premise of orchard-robot navigation literature. Three precisions are needed, none of which overturn …"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "verify:5",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ac1290ce26ce02368",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784902770527,
      "queuedAt": 1784902767396,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "FAST-LIO2 / Point-LIO are LiDAR-inertial odometry packages …",
      "promptPreview": "TARGET MACHINE (verified, ground truth): Ubuntu 24.04.4 noble, i5-13500 (20 threads), 31 GiB RAM,\nGTX 1660 SUPER (TU116, no RT cores) with a currently-BROKEN driver (Secure Boot blocks the DKMS module),\nIntel Alder Lake iGPU as fallback, Wayland session, no ROS/Gazebo/Docker installed yet.\nPlanned stack: ROS 2 Jazzy + Gazebo Harmonic (gz-sim 8).\n\nPROJECT: An AgileX SCOUT MINI (4WD skid-steer) carr…",
      "lastProgressAt": 1784903062843,
      "tokens": 44038,
      "toolCalls": 28,
      "durationMs": 292315,
      "resultPreview": "{\"claim\":\"FAST-LIO2 / Point-LIO are LiDAR-inertial odometry packages that Livox sensors are commonly used with, and they can operate with the narrow FOV of a Livox solid-state LiDAR.\",\"verdict\":\"CONFIRMED\",\"correction\":\"The claim as literally stated is accurate and backed by primary sources. No correction to the claim itself is needed. However, it is stated at a level of generality that hides four…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "verify:6",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a89d1d95f14e1a52b",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784902769603,
      "queuedAt": 1784902767396,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Modern high-density tall-spindle apple orchards typically u…",
      "promptPreview": "TARGET MACHINE (verified, ground truth): Ubuntu 24.04.4 noble, i5-13500 (20 threads), 31 GiB RAM,\nGTX 1660 SUPER (TU116, no RT cores) with a currently-BROKEN driver (Secure Boot blocks the DKMS module),\nIntel Alder Lake iGPU as fallback, Wayland session, no ROS/Gazebo/Docker installed yet.\nPlanned stack: ROS 2 Jazzy + Gazebo Harmonic (gz-sim 8).\n\nPROJECT: An AgileX SCOUT MINI (4WD skid-steer) carr…",
      "lastProgressAt": 1784902978710,
      "tokens": 39750,
      "toolCalls": 17,
      "durationMs": 209107,
      "resultPreview": "{\"claim\":\"Modern high-density tall-spindle apple orchards typically use row spacing on the order of 3 to 4 metres and in-row tree spacing on the order of 0.8 to 1.5 metres.\",\"verdict\":\"CONFIRMED\",\"correction\":\"The claim survives scrutiny — every primary source I found falls inside or within centimetres of the stated envelope. Two precision refinements, neither of which overturns it:\\n\\n(1) ROW SPA…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "verify:7",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a7ee98df7a856ee56",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784902769788,
      "queuedAt": 1784902767396,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Nav2 on ROS 2 Jazzy provides the MPPI controller and the Sm…",
      "promptPreview": "TARGET MACHINE (verified, ground truth): Ubuntu 24.04.4 noble, i5-13500 (20 threads), 31 GiB RAM,\nGTX 1660 SUPER (TU116, no RT cores) with a currently-BROKEN driver (Secure Boot blocks the DKMS module),\nIntel Alder Lake iGPU as fallback, Wayland session, no ROS/Gazebo/Docker installed yet.\nPlanned stack: ROS 2 Jazzy + Gazebo Harmonic (gz-sim 8).\n\nPROJECT: An AgileX SCOUT MINI (4WD skid-steer) carr…",
      "lastProgressAt": 1784903051611,
      "tokens": 45327,
      "toolCalls": 22,
      "durationMs": 281822,
      "resultPreview": "{\"claim\":\"Nav2 on ROS 2 Jazzy provides the MPPI controller and the Smac Planner family, and these are better suited to a skid-steer robot navigating narrow orchard rows than the legacy DWB controller.\",\"verdict\":\"PARTIALLY_TRUE\",\"correction\":\"Three separable assertions; only the first survives intact.\\n\\n1) AVAILABILITY — CONFIRMED. Both `nav2_mppi_controller` and `nav2_smac_planner` are released …"
    },
    {
      "type": "workflow_agent",
      "index": 11,
      "label": "synthesize-addendum",
      "phaseIndex": 3,
      "phaseTitle": "Synthesize",
      "agentId": "adfd8145bd0feece9",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784903214761,
      "queuedAt": 1784903211204,
      "attempt": 1,
      "promptPreview": "TARGET MACHINE (verified, ground truth): Ubuntu 24.04.4 noble, i5-13500 (20 threads), 31 GiB RAM,\nGTX 1660 SUPER (TU116, no RT cores) with a currently-BROKEN driver (Secure Boot blocks the DKMS module),\nIntel Alder Lake iGPU as fallback, Wayland session, no ROS/Gazebo/Docker installed yet.\nPlanned stack: ROS 2 Jazzy + Gazebo Harmonic (gz-sim 8).\n\nPROJECT: An AgileX SCOUT MINI (4WD skid-steer) carr…",
      "lastProgressAt": 1784903657156,
      "tokens": 86746,
      "toolCalls": 0,
      "durationMs": 442395,
      "resultPreview": "# ADDENDUM DECISION BRIEF\n## AgileX SCOUT MINI + Livox MID-70, Korean high-density apple orchard (세장방추형), ROS 2 Jazzy + Gazebo Harmonic\n\n**Standing rule applied throughout: where a verification verdict refutes or corrects a research finding, the VERDICT WINS.** Every place that happened is called out explicitly with a `VERDICT OVERRIDE` tag.\n\n---\n\n## 0. The six verdict overrides that changed the d…"
    }
  ],
  "totalTokens": 609848,
  "totalToolCalls": 289
}